---
phase: 06-pok-dex
plan: 06
type: execute
wave: 5
depends_on: [06-03, 06-04, 06-05]
files_modified:
  - pokemon-safari-app/src/utils/relativeDay.ts
  - pokemon-safari-app/src/utils/relativeDay.test.ts
  - pokemon-safari-app/src/components/dex/DexDetailSheet.tsx
  - pokemon-safari-app/src/components/dex/DexTile.test.tsx
  - pokemon-safari-app/src/components/QuotaNote.tsx
  - pokemon-safari-app/src/screens/DexScreen.tsx
  - pokemon-safari-app/src/screens/DexScreen.test.tsx
  - .planning/phases/06-pok-dex/06-VALIDATION.md
autonomous: true
requirements: [DEX-01, DEX-02, DEX-03]

must_haves:
  truths:
    - "The caught detail sheet shows sprite, name, sanitized Emerald-preferring flavor, Caught n, First seen and First caught relative dates (D-03, D-15 – D-17, D-20)"
    - "When shinyOwned, the sheet opens on the shiny sprite with a Show normal toggle that swaps the image only — no save write (D-09)"
    - "flavorText null renders the kid-friendly placeholder No Pokédex entry yet. at the UI layer only (D-16)"
    - "Relative dates use local calendar days: Today / Yesterday / N days ago / short date / Sometime (D-20)"
    - "A save-quota soft-fail shows a gentle dismissible Pokédex note and play continues; no storage key is cleared (D-21)"
    - "Caught and stub sheets keep BottomNav inert while open (UI-SPEC Motion/A11y — Plan 03 wiring preserved)"
    - "The full automated suite is green and 06-VALIDATION.md records the phase gate"
  artifacts:
    - path: "pokemon-safari-app/src/utils/relativeDay.ts"
      provides: "formatRelativeDay local-calendar kid copy"
      exports: ["formatRelativeDay"]
    - path: "pokemon-safari-app/src/components/dex/DexDetailSheet.tsx"
      provides: "Full caught detail branch beside the leak-free stub"
      contains: "Show shiny"
    - path: "pokemon-safari-app/src/components/QuotaNote.tsx"
      provides: "Optional message override so Dex can supply save-specific copy"
    - path: ".planning/phases/06-pok-dex/06-VALIDATION.md"
      provides: "Closed-out verification map and phase gate"
  key_links:
    - from: "pokemon-safari-app/src/components/dex/DexDetailSheet.tsx"
      to: "pokemon-safari-app/src/utils/relativeDay.ts"
      via: "First seen / First caught labels"
      pattern: "formatRelativeDay"
    - from: "pokemon-safari-app/src/components/dex/DexDetailSheet.tsx"
      to: "pokemon-safari-app/src/services/pokeapi/cache.ts"
      via: "getPokemon supplies name, sprites, and flavorText from cache v2"
      pattern: "flavorText"
    - from: "pokemon-safari-app/src/screens/DexScreen.tsx"
      to: "pokemon-safari-app/src/components/QuotaNote.tsx"
      via: "renders on useDexStore saveSoftFail with the Pokédex message"
      pattern: "saveSoftFail"
    - from: "pokemon-safari-app/src/screens/DexScreen.tsx"
      to: "pokemon-safari-app/src/components/BottomNav.tsx"
      via: "preserves dexSheetOpen → navInert while caught/stub sheet open"
      pattern: "dexSheetOpen"
---

## Phase Goal

**As a** child catching Pokémon on Safari, **I want to** open a Gen 1 Pokédex where unknowns stay silhouettes until I catch them and my Seen/Caught progress is visible, **so that** the endless loop has a collection reward that drives me back into the grass.

**This plan's slice:** the payoff screen — a caught entry finally tells its story (lore, shiny sprite, "First caught: Yesterday"), and a full storage device fails gently instead of scaring a kid.

> **Split note (scope-sanity gate):** this is the detail-UI half of the original 16-file `06-04`, now also carrying `formatRelativeDay` (its only consumer is this sheet). It closes the phase.

<objective>
Implement `formatRelativeDay`, complete the `DexDetailSheet` caught branch with lore, meta, and the shiny toggle, extend `QuotaNote` with an overridable message wired to the dex save soft-fail, and close out `06-VALIDATION.md` with a green full suite (D-03, D-09, D-16, D-20, D-21).

Purpose: DEX-01/02/03 become observably true end-to-end for a player, not just for unit tests.
Output: green `relativeDay.test.ts`, caught-detail assertions in `DexScreen.test.tsx` / `DexTile.test.tsx`, and `npm test` green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pok-dex/06-CONTEXT.md
@.planning/phases/06-pok-dex/06-RESEARCH.md
@.planning/phases/06-pok-dex/06-PATTERNS.md
@.planning/phases/06-pok-dex/06-UI-SPEC.md
@.planning/phases/06-pok-dex/06-VALIDATION.md
@.planning/phases/06-pok-dex/06-03-SUMMARY.md
@.planning/phases/06-pok-dex/06-04-SUMMARY.md
@.planning/phases/06-pok-dex/06-05-SUMMARY.md
@pokemon-safari-app/src/components/dex/DexDetailSheet.tsx
@pokemon-safari-app/src/components/QuotaNote.tsx
@pokemon-safari-app/src/screens/DexScreen.tsx
@pokemon-safari-app/src/screens/GameScreen.tsx
@pokemon-safari-app/src/utils/rng.ts
</context>

## Artifacts this phase produces

- `utils/relativeDay.ts` — `formatRelativeDay(iso, now = new Date())`
- `DexDetailSheet` caught branch — sprite + shiny toggle (`Show shiny` / `Show normal`), name, `#NNN`, flavor or placeholder, `Caught`, `First seen`, `First caught`, `Close`
- `QuotaNote` — optional `message` prop (default keeps the Phase 2 poke-cache copy)
- `06-VALIDATION.md` — statuses filled, `nyquist_compliant: true` at the phase gate

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relative-day util + caught detail lore UI</name>
  <files>pokemon-safari-app/src/utils/relativeDay.ts, pokemon-safari-app/src/utils/relativeDay.test.ts, pokemon-safari-app/src/components/dex/DexDetailSheet.tsx, pokemon-safari-app/src/components/dex/DexTile.test.tsx</files>
  <read_first>
    - pokemon-safari-app/src/utils/relativeDay.test.ts
    - pokemon-safari-app/src/utils/rng.ts
    - pokemon-safari-app/src/components/dex/DexDetailSheet.tsx
    - pokemon-safari-app/src/components/PixelButton.tsx
    - pokemon-safari-app/src/components/PokemonSprite.tsx
    - pokemon-safari-app/src/components/dex/DexTile.test.tsx
    - .planning/phases/06-pok-dex/06-UI-SPEC.md (caught detail layout, copy table, shiny toggle default, a11y)
    - .planning/phases/06-pok-dex/06-RESEARCH.md (Pattern 5 relative dates, Pitfall 8)
  </read_first>
  <behavior>
    - formatRelativeDay: same local calendar day → Today; 1 → Yesterday; 2–6 → N days ago; ≥7 → short month/day date; unparseable ISO → Sometime
    - Boundary cases with a fixed now at 23:59 local and across a month boundary behave by calendar day, not by 24-hour windows
    - Caught sheet renders the 96px sprite, name, #NNN, flavor text, Caught: n, First seen: rel, First caught: rel, and Close
    - flavorText null renders No Pokédex entry yet.
    - shinyOwned entries open on the shiny sprite with a Show normal toggle; toggling swaps the sprite URL only
    - The stub branch stays leak-free for uncaught ids
  </behavior>
  <action>
    Implement `utils/relativeDay.ts` as a pure util in the `utils/rng.ts` shape — `formatRelativeDay(iso: string, now: Date = new Date())`, comparing local year/month/date triples rather than millisecond diffs (Pitfall 8: a UTC-based diff flips "Today" to "Yesterday" at 7pm EST), with `Number.isNaN(Date.parse(iso))` guarded to the fallback copy. Cutoff at 7 days → `toLocaleDateString(undefined, { month: 'short', day: 'numeric' })` per the UI-SPEC discretionary choice; the stored value stays an ISO timestamp and only the display is relativized (D-20). Complete the `DexDetailSheet` caught branch per the UI-SPEC layout: `getPokemon(speciesId)` supplies name, sprites, and the cache-v2 `flavorText`; the entry supplies `catchCount` and the two timestamps — together these are exactly the D-03 reveal set (sprite, name, flavor, number caught, shiny owned, first encountered, first captured). Render the `No Pokédex entry yet.` placeholder when `flavorText` is null — the kid-friendly fallback is UI-only per D-16, never written into the cache. Add the shiny toggle as `PixelButton`s labeled `Show shiny` / `Show normal`, defaulting to the shiny sprite when `entry.shinyOwned` (D-09 detail-side toggle, UI-SPEC discretionary D7); the toggle is view state only and must not trigger a save write. Render flavor as JSX text nodes only — no `dangerouslySetInnerHTML` anywhere in this file. Leave the stub branch exactly as `06-03` built it and extend `DexTile.test.tsx` for the caught/sparkle path.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npx vitest run src/utils/relativeDay.test.ts src/components/dex/DexTile.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - relativeDay.ts exports formatRelativeDay
    - relativeDay.test.ts covers Today, Yesterday, N days ago, and the invalid-input fallback
    - DexDetailSheet.tsx contains Show shiny or Show normal, First seen, and formatRelativeDay
    - `grep -c 'dangerouslySetInnerHTML' src/components/dex/DexDetailSheet.tsx` is 0
    - Listed vitest command exits 0
  </acceptance_criteria>
  <done>A caught entry reads like a real Pokédex page with kid-friendly dates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dex quota note, screen wiring, and phase closeout</name>
  <files>pokemon-safari-app/src/components/QuotaNote.tsx, pokemon-safari-app/src/screens/DexScreen.tsx, pokemon-safari-app/src/screens/DexScreen.test.tsx, .planning/phases/06-pok-dex/06-VALIDATION.md</files>
  <read_first>
    - pokemon-safari-app/src/components/QuotaNote.tsx
    - pokemon-safari-app/src/screens/GameScreen.tsx
    - pokemon-safari-app/src/screens/DexScreen.tsx
    - pokemon-safari-app/src/screens/DexScreen.test.tsx
    - pokemon-safari-app/src/store/dexStore.ts
    - .planning/phases/06-pok-dex/06-UI-SPEC.md (quota copy, sheet open path)
    - .planning/phases/06-pok-dex/06-VALIDATION.md
  </read_first>
  <behavior>
    - QuotaNote without the new prop renders its existing poke-cache sentence, so BootScreen and GameScreen are unchanged
    - DexScreen renders QuotaNote with Couldn't save your Pokédex on this device. You can still play this visit. when saveSoftFail is true, and Got it dismisses it
    - Opening a caught tile shows the full detail fields; opening an uncaught tile still shows only the stub
    - Opening either sheet still sets Main BottomNav inert on /dex and clears it on close — preserve the Plan 03 dexSheetOpen → BottomNav wiring (UI-SPEC Motion/A11y)
    - The full suite passes
  </behavior>
  <action>
    Extend `QuotaNote` with an optional `message?: string` defaulting to the existing Phase 2 poke-cache sentence so no current caller changes, and keep the `Got it` dismiss affordance. Wire `DexScreen` to render it with the Pokédex-specific sentence whenever `useDexStore` reports `saveSoftFail`, dismissing via `dismissSaveSoftFail` — this is the D-21 gentle-note contract: the note is non-blocking, in-memory play continues, the next write retries, and nothing clears poke-cache or edu-stats. Keep the Plan 03 `dexSheetOpen` / `setDexSheetOpen` BottomNav inert contract intact when extending DexScreen (UI-SPEC Accessibility: BottomNav inert while sheet open) — caught and stub opens both set the flag; Close/Escape/scrim and unmount still clear it; do not regress `navInert` on `/dex`. Extend `DexScreen.test.tsx` with the caught-detail open path (flavor or placeholder, meta rows, shiny toggle when owned), a soft-fail case asserting the Pokédex copy, and a caught-sheet inert assertion (Main nav has `inert` while open, cleared after Close — same as the stub path from Plan 03). Then close out `06-VALIDATION.md`: mark each Per-Task Verification Map row's Status from the actual runs, tick the Wave 0 Requirements checklist, complete the Validation Sign-Off, and set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter once the full suite is green. Run `npm test` last and fix any residual fallout from the cache-v2 helper bump before declaring done.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm test</automated>
  </verify>
  <acceptance_criteria>
    - QuotaNote accepts a message override and its default copy is unchanged
    - DexScreen.tsx references saveSoftFail and passes a message containing Pokédex
    - DexScreen.tsx still sets/clears dexSheetOpen around sheet open/close/unmount
    - DexScreen.test.tsx covers the caught-detail open path, the save soft-fail note, and Main nav inert while the caught sheet is open
    - 06-VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Caught lore, gentle quota UX, and a green phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poke-cache flavorText → DexDetailSheet | A remote-origin string is rendered into the DOM |
| dexStore saveSoftFail → DexScreen | Storage failure state drives user-facing copy and must never escalate to a clear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering / XSS | DexDetailSheet flavor rendering | mitigate | JSX text interpolation only; sanitizeFlavorText already stripped control chars; grep gate forbids dangerouslySetInnerHTML |
| T-06-10 | Information Disclosure | stub vs caught branch | mitigate | Stub path still forbids name, sprite, and flavor; tests assert ??? only for uncaught ids |
| T-06-05 | Denial of Service | QuotaNote on saveSoftFail | mitigate | Non-blocking dismissible note; play continues from memory; no storage clear (D-21) |
| T-06-16 | Tampering | corrupt timestamps reaching formatRelativeDay | mitigate | Invalid ISO guarded to fallback copy; the save sanitizer already rejects non-string timestamps |
| T-06-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
Full `npm test` green; Dex caught detail shows lore and relative dates; quota note uses Pokédex copy; VALIDATION closed out.
</verification>

<success_criteria>
- Caught detail shows the full D-03 field set with kid-friendly relative dates and a shiny toggle
- Null flavor degrades to the placeholder without leaking into stub entries
- Save quota surfaces a gentle Pokédex note with no collateral storage damage
- DEX-01, DEX-02, DEX-03 are observably true end-to-end and the suite is green
</success_criteria>

<output>
Create `.planning/phases/06-pok-dex/06-06-SUMMARY.md` when done
</output>
