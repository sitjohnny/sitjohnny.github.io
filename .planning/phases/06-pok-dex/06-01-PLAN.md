---
phase: 06-pok-dex
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pokemon-safari-app/src/game/dex.test.ts
  - pokemon-safari-app/src/services/save.test.ts
  - pokemon-safari-app/src/store/dexStore.test.ts
  - pokemon-safari-app/src/utils/relativeDay.test.ts
  - pokemon-safari-app/src/services/pokeapi/flavorText.test.ts
  - pokemon-safari-app/src/screens/DexScreen.test.tsx
  - pokemon-safari-app/src/components/dex/DexTile.test.tsx
  - pokemon-safari-app/src/data/config-surface.test.ts
  - .planning/phases/06-pok-dex/06-VALIDATION.md
autonomous: true
requirements: [DEX-01, DEX-02, DEX-03]

must_haves:
  truths:
    - "Wave 0 Vitest files exist and fail for missing dex reducers, save service, flavor helpers, relative-day util, dexStore debounce, and DexScreen grid/header/sheet (DEX-01/02/03)"
    - "config-surface tests assert shinyRate and dexSaveDebounceMs will live under data/ (DATA-03)"
    - "06-VALIDATION.md Wave 0 checklist rows list concrete automated commands"
  artifacts:
    - path: "pokemon-safari-app/src/game/dex.test.ts"
      provides: "Failing contracts for markSeen, recordCatch, dexTileState, countSeen/countCaught"
    - path: "pokemon-safari-app/src/services/save.test.ts"
      provides: "Failing SAVE_KEY round-trip + quota + neighbor-key isolation"
    - path: "pokemon-safari-app/src/screens/DexScreen.test.tsx"
      provides: "Failing DEX-03 browse/header/stub-sheet assertions"
    - path: ".planning/phases/06-pok-dex/06-VALIDATION.md"
      provides: "Updated per-task verification map"
  key_links:
    - from: "pokemon-safari-app/src/game/dex.test.ts"
      to: "pokemon-safari-app/src/game/dex.ts"
      via: "imports markSeen/recordCatch/dexTileState (modules absent → RED)"
      pattern: "markSeen|recordCatch|dexTileState"
    - from: "pokemon-safari-app/src/services/save.test.ts"
      to: "pokemon-safari-app/src/services/pokeapi/keys.ts"
      via: "SAVE_KEY neighbor isolation vs CACHE_KEY and EDU_STATS_KEY"
      pattern: "SAVE_KEY"
---

## Phase Goal

**As a** child catching Pokémon on Safari, **I want to** open a Gen 1 Pokédex where unknowns stay silhouettes until I catch them and my Seen/Caught progress is visible, **so that** the endless loop has a collection reward that drives me back into the grass.

> Source note: `ROADMAP.md` Phase 6 Goal is outcome-shaped. The story above renders that goal plus success criteria and CONTEXT D-01…D-21; it introduces no new scope.

**This plan's slice:** Wave 0 failing tests only — pin DEX-01/02/03 contracts before any production code. Implementation starts in `06-02`.

<objective>
Author failing Vitest suites for pure dex reducers/selectors (D-06/D-07/D-09), SaveEnvelope dex-slice I/O + quota (D-18/D-21), flavor selection/sanitize (D-15–D-17), relative-day formatting (D-20), dexStore debounce flush (D-19), DexScreen browse/stub sheet (DEX-03), and config-surface keys for shinyRate + dexSaveDebounceMs.

Purpose: Nyquist Wave 0 — every later plan turns a known RED suite green without inventing behavior mid-flight.
Output: failing test files + updated 06-VALIDATION.md checklist; no production modules yet.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pok-dex/06-CONTEXT.md
@.planning/phases/06-pok-dex/06-RESEARCH.md
@.planning/phases/06-pok-dex/06-PATTERNS.md
@.planning/phases/06-pok-dex/06-UI-SPEC.md
@.planning/phases/06-pok-dex/06-VALIDATION.md
@pokemon-safari-app/src/game/education/adaptiveStore.test.ts
@pokemon-safari-app/src/game/capture.test.ts
@pokemon-safari-app/src/data/config-surface.test.ts
@pokemon-safari-app/src/screens/BootScreen.test.tsx
@pokemon-safari-app/src/services/pokeapi/keys.ts
@pokemon-safari-app/src/types/save.ts
</context>

## Artifacts this phase produces

Symbols/files introduced across Phase 6 plans (this Wave 0 only creates the test files that import them):

- `game/dex.ts` — `markSeen`, `recordCatch`, `emptyDexEntry`, `dexTileState`, `countSeen`, `countCaught`, `DexTileState`
- `services/save.ts` — `loadSave`, `persistSave`, `resetSaveForTests`, `parseSaveEnvelope` (or equivalent private parse)
- `store/dexStore.ts` — `useDexStore`, `markSeen`, `recordCatch`, `dismissSaveSoftFail`, `flushNow` / schedule helpers
- `utils/relativeDay.ts` — `formatRelativeDay`
- `services/pokeapi/client.ts` — `selectFlavorText`, `sanitizeFlavorText`, `fetchSpeciesFlavor` (or merged fetch)
- `components/dex/DexHeader.tsx`, `DexGrid.tsx`, `DexTile.tsx`, `DexDetailSheet.tsx`
- `data/rates.ts` keys — `shinyRate`, `dexSaveDebounceMs`
- CSS — `.sprite-silhouette`, `.dex-tile-caught`, `.dex-sparkle`
- Types — `DexEntry`, `DexData`, `PokemonDto.flavorText`, `CacheEnvelopeV2`, `EncounterSession.shiny`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 failing pure-logic tests (dex, save, flavor, relativeDay, config)</name>
  <files>pokemon-safari-app/src/game/dex.test.ts, pokemon-safari-app/src/services/save.test.ts, pokemon-safari-app/src/utils/relativeDay.test.ts, pokemon-safari-app/src/services/pokeapi/flavorText.test.ts, pokemon-safari-app/src/data/config-surface.test.ts</files>
  <read_first>
    - pokemon-safari-app/src/game/education/adaptiveStore.test.ts
    - pokemon-safari-app/src/game/capture.test.ts
    - pokemon-safari-app/src/data/config-surface.test.ts
    - pokemon-safari-app/src/services/pokeapi/keys.ts
    - pokemon-safari-app/src/types/save.ts
    - .planning/phases/06-pok-dex/06-RESEARCH.md (Pattern 1, Pattern 3, Pattern 5 selectors, Pitfall 5)
    - .planning/phases/06-pok-dex/06-CONTEXT.md (D-06…D-21)
    - .planning/phases/06-pok-dex/06-UI-SPEC.md (Copywriting Contract relative-day rows)
  </read_first>
  <behavior>
    - markSeen: first call sets seen=true + firstEncounteredAt ISO; second call same species is idempotent (same object reference or deep-equal unchanged)
    - recordCatch: increments catchCount, sets firstCapturedAt once, ORs shinyOwned, forces seen=true, fills firstEncounteredAt if missing
    - dexTileState: absent or seen-only entry → kind 'unknown' with label `#NNN Pokémon` (no species name); caught → kind 'caught' with name + shinyOwned flag (D-06/D-07/D-09)
    - countSeen / countCaught: count entries with seen===true / firstCapturedAt!==null against GEN1_COUNT denominator used by UI later
    - loadSave/persistSave: round-trip DexData under SAVE_KEY version 1 envelope; corrupt/missing → {}; QuotaExceededError mock → 'quota'; CACHE_KEY and EDU_STATS_KEY untouched
    - selectFlavorText: emerald en preferred; then ruby/sapphire/firered; then any en; else null; sanitizeFlavorText collapses \n/\f/whitespace (D-15–D-17)
    - formatRelativeDay: Today / Yesterday / N days ago (2–6) / short date ≥7 / Sometime on invalid ISO — local calendar days with injected now (D-20, UI-SPEC)
    - config-surface: shinyRate and dexSaveDebounceMs exported from data/rates (or adjacent data module) — assert presence (may fail until 06-03 adds keys; keep assertion in this file so 06-03 must satisfy it)
  </behavior>
  <action>
    Create the listed test files importing the future modules/symbols named in Artifacts. Do not implement production modules in this plan — imports must fail or expectations must fail (RED). Mirror adaptiveStore.test neighbor-key isolation: seed CACHE_KEY + EDU_STATS_KEY, assert unchanged after save exercises; never call or comment `localStorage.clear(`. Use fixture flavor entries with version names emerald/ruby/sapphire/firered and language.name en. Relative-day cases must fix `now` near local midnight edges. Extend config-surface.test.ts to expect `shinyRate` (number in (0,1]) and `dexSaveDebounceMs` (positive number, RESEARCH discretionary default 800). Cite D-06, D-07, D-15, D-16, D-17, D-18, D-20, D-21 in describe titles where helpful.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npx vitest run src/game/dex.test.ts src/services/save.test.ts src/utils/relativeDay.test.ts src/services/pokeapi/flavorText.test.ts src/data/config-surface.test.ts 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - Files exist at the paths listed in files
    - Command exits non-zero OR reports failing tests (RED) — not silent skip
    - save.test.ts source contains SAVE_KEY and does not contain the substring localStorage.clear(
    - dex.test.ts source contains markSeen, recordCatch, and dexTileState
    - flavorText.test.ts source contains emerald and sanitizeFlavorText or selectFlavorText
    - relativeDay.test.ts source contains Today and Yesterday string expectations
  </acceptance_criteria>
  <done>Pure-logic Wave 0 suites exist and fail pending 06-02/06-03/06-04 implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 failing UI/store tests + VALIDATION map</name>
  <files>pokemon-safari-app/src/store/dexStore.test.ts, pokemon-safari-app/src/screens/DexScreen.test.tsx, pokemon-safari-app/src/components/dex/DexTile.test.tsx, .planning/phases/06-pok-dex/06-VALIDATION.md</files>
  <read_first>
    - pokemon-safari-app/src/screens/BootScreen.test.tsx
    - pokemon-safari-app/src/components/PokemonSprite.test.tsx
    - pokemon-safari-app/src/hooks/useEncounterFlow.test.ts
    - pokemon-safari-app/src/screens/DexScreen.tsx
    - .planning/phases/06-pok-dex/06-UI-SPEC.md (Screen Inventory, Copywriting, Component Inventory)
    - .planning/phases/06-pok-dex/06-VALIDATION.md
    - .planning/phases/06-pok-dex/06-PATTERNS.md (DexDetailSheet / DexTile analogs)
  </read_first>
  <behavior>
    - dexStore: markSeen/recordCatch update in-memory dex; after advanceTimersByTime(dexSaveDebounceMs) SAVE_KEY receives envelope; quota path sets saveSoftFail true without throwing
    - DexScreen: renders heading Pokédex; text matching Seen 0/151 · Caught 0/151 (or seeded counts); 151 tile buttons; tap uncaught opens dialog with ??? and Not caught yet; Close dismisses; no EmptyState placeholder
    - DexTile: unknown state accessible name is Pokémon #NNN without species name; caught state includes name; shinyOwned includes shiny owned in name / sparkle present
  </behavior>
  <action>
    Write failing component/store tests per UI-SPEC copy strings exactly: Pokédex, Seen {s}/151 · Caught {c}/151, ???, Not caught yet. Catch one in the grass to reveal this entry., Close. Seed poke-cache via existing makePokemonDto/seedPokeCache helpers so tile sprites can resolve once implementation lands. Use vi.useFakeTimers for debounce tests and restore in afterEach (Pitfall 4). Update 06-VALIDATION.md: set wave_0_complete aspirational rows to concrete Task IDs 06-01-T1/T2, fill Per-Task Verification Map with the automated commands from RESEARCH Validation Architecture, leave Status pending/red, set nyquist_compliant false until phase verify. Do not implement DexScreen/components yet.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npx vitest run src/store/dexStore.test.ts src/screens/DexScreen.test.tsx src/components/dex/DexTile.test.tsx 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - DexScreen.test.tsx exists and references Seen and 151
    - DexTile.test.tsx exists and asserts no name leak pattern (Pokémon #)
    - dexStore.test.ts references fake timers or advanceTimersByTime
    - 06-VALIDATION.md Per-Task Verification Map no longer has only TBD placeholder rows — includes concrete file paths from this plan
    - Vitest run for these three files fails (RED)
  </acceptance_criteria>
  <done>UI/store Wave 0 suites + VALIDATION map ready for 06-02+.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → localStorage | Seeded/corrupt envelopes exercise parse paths that production will trust only after sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | save.test.ts fixtures | mitigate | Tests require corrupt envelopes → {}; invalid catchCount/timestamps dropped — production save.ts must match |
| T-06-02 | Information Disclosure | DexTile.test.tsx | mitigate | Assert uncaught a11y names never include species name (Pitfall 7) |
| T-06-SC | Tampering | npm installs | accept | No new packages in this phase — legitimacy gate vacuously satisfied |
</threat_model>

<verification>
Wave 0 files exist; targeted vitest runs are RED; VALIDATION map updated; no production dex modules committed yet.
</verification>

<success_criteria>
- All Wave 0 test files listed in files_modified exist
- Suites fail for missing implementations (not pass vacuously)
- 06-VALIDATION.md documents concrete automated commands for DEX-01/02/03 seams
</success_criteria>

<output>
Create `.planning/phases/06-pok-dex/06-01-SUMMARY.md` when done
</output>
