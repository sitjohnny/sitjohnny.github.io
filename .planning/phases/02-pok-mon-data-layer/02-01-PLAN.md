---
phase: 02-pok-mon-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pokemon-safari-app/src/services/pokeapi/cache.test.ts
  - pokemon-safari-app/src/services/pokeapi/client.test.ts
  - pokemon-safari-app/src/services/pokeapi/keys.test.ts
  - pokemon-safari-app/src/screens/BootScreen.test.tsx
  - pokemon-safari-app/src/screens/GameScreen.test.tsx
  - pokemon-safari-app/src/components/PokemonSprite.test.tsx
  - pokemon-safari-app/src/test/pokeapi-test-helpers.ts
autonomous: true
requirements:
  - BOOT-04
  - DATA-01
  - DATA-02
  - DATA-04
must_haves:
  truths:
    - "Wave 0 Vitest files exist for cache, client, keys, BootScreen, GameScreen, and PokemonSprite"
    - "Service tests assert versioned cache write, zero fetch after hydrate, key isolation, quota soft-fail, and version mismatch re-fetch"
    - "Component tests assert Boot progress/retry, Game gate when not ready, and pixelated sprite fallback"
  artifacts:
    - path: "pokemon-safari-app/src/services/pokeapi/cache.test.ts"
      provides: "Failing DATA-01/02/04 + D-03/D-06/D-10 contracts"
    - path: "pokemon-safari-app/src/screens/BootScreen.test.tsx"
      provides: "Failing BOOT-04 progress + retry contract"
    - path: "pokemon-safari-app/src/screens/GameScreen.test.tsx"
      provides: "Failing D-02 Game gate contract"
    - path: "pokemon-safari-app/src/components/PokemonSprite.test.tsx"
      provides: "Failing D-07/D-08/D-09 sprite contract"
  key_links:
    - from: "pokemon-safari-app/src/services/pokeapi/cache.test.ts"
      to: "ensureCache / getPokemon / hasValidCache"
      via: "imports from @/services/pokeapi/cache (may fail resolve until 02-02)"
      pattern: "ensureCache|getPokemon|hasValidCache"
    - from: "pokemon-safari-app/src/test/pokeapi-test-helpers.ts"
      to: "CACHE_KEY only"
      via: "seed/clear poke-cache key — never teach production localStorage.clear"
      pattern: "poke-cache"
---

## Phase Goal

**As a** player, **I want to** prefetch Gen 1 Pokémon data once into a versioned local cache behind a Boot screen, **so that** gameplay never touches the network.

<objective>
Land Wave 0 failing Vitest contracts for the PokéAPI cache, Boot/Game UX, and PokemonSprite so later vertical slices turn RED→GREEN against locked D-01..D-11 and BOOT-04/DATA-01/02/04.

Purpose: Nyquist sampling for Phase 2 — every requirement has an automated failing test before implementation (per 02-VALIDATION.md).
Output: Six test files + shared pokeapi test helpers; suite runs and fails until plans 02-02..02-04 implement.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pok-mon-data-layer/02-CONTEXT.md
@.planning/phases/02-pok-mon-data-layer/02-RESEARCH.md
@.planning/phases/02-pok-mon-data-layer/02-PATTERNS.md
@.planning/phases/02-pok-mon-data-layer/02-UI-SPEC.md
@.planning/phases/02-pok-mon-data-layer/02-VALIDATION.md
@pokemon-safari-app/src/App.test.tsx
@pokemon-safari-app/src/components/BottomNav.test.tsx
@pokemon-safari-app/src/test/setup.ts
</context>

## Artifacts this phase produces

Symbols and paths introduced across Phase 2 plans (declared here for source-grounding; Wave 0 only creates the test/helper artifacts):

- `pokemon-safari-app/src/test/pokeapi-test-helpers.ts` — `seedPokeCache`, `clearPokeCacheKey`, `stubPokeApiFetch`
- `pokemon-safari-app/src/services/pokeapi/keys.ts` — `CACHE_VERSION`, `CACHE_KEY`, `SAVE_KEY`
- `pokemon-safari-app/src/types/pokemon.ts` — `PokemonDto`, `CacheEnvelopeV1`, `CacheEnvelope`
- `pokemon-safari-app/src/services/pokeapi/client.ts` — `toPokemonDto`, `fetchPokemon`, `fetchGen1All`, `mapPool`
- `pokemon-safari-app/src/services/pokeapi/cache.ts` — `hydrateFromStorage`, `hasValidCache`, `isCacheReady`, `getPokemon`, `persistCache`, `ensureCache`, `resetCacheMemoryForTests`
- `pokemon-safari-app/src/hooks/usePokemonCache.ts` — `usePokemonCache`
- `pokemon-safari-app/src/screens/BootScreen.tsx` — `BootScreen`
- `pokemon-safari-app/src/components/PokemonSprite.tsx` — `PokemonSprite`
- `pokemon-safari-app/src/components/QuotaNote.tsx` — `QuotaNote`
- Modified: `GameScreen` gate branch, `App`/`createAppRouter` boot route + sync hydrate entry

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 pokeapi service tests and helpers</name>
  <files>pokemon-safari-app/src/test/pokeapi-test-helpers.ts, pokemon-safari-app/src/services/pokeapi/keys.test.ts, pokemon-safari-app/src/services/pokeapi/client.test.ts, pokemon-safari-app/src/services/pokeapi/cache.test.ts</files>
  <read_first>.planning/phases/02-pok-mon-data-layer/02-VALIDATION.md, .planning/phases/02-pok-mon-data-layer/02-RESEARCH.md, .planning/phases/02-pok-mon-data-layer/02-CONTEXT.md, pokemon-safari-app/src/components/BottomNav.test.tsx, pokemon-safari-app/src/test/setup.ts</read_first>
  <behavior>
    - keys.test: CACHE_KEY is pokemon-safari:poke-cache:v1; SAVE_KEY is pokemon-safari:save:v1; keys are distinct (DATA-04, D-11).
    - client.test: toPokemonDto sorts types by slot and maps front_default + front_shiny; fetchGen1All with mocked fetch uses concurrency ≤8 and reports onProgress (done,total).
    - cache.test: ensureCache writes CacheEnvelope version 1 with 151 DTOs to CACHE_KEY only (DATA-01); after hydrate, getPokemon(id) works and fetch call count does not increase (DATA-02, D-03); QuotaExceededError on setItem returns quota path without throw and leaves memory playable (D-06); wrong version envelope fails hasValidCache and ensureCache re-fetches (D-10); production modules under src/services must not call localStorage.clear (assert via grep in test or separate expect on helper-only clear).
    - Helpers: stubPokeApiFetch returns ok JSON per id; clearPokeCacheKey removes only CACHE_KEY; seedPokeCache writes a valid envelope — never call localStorage.clear in helpers used as production patterns (test isolation may removeItem CACHE_KEY and SAVE_KEY markers only).
  </behavior>
  <action>
    Create Wave 0 failing unit tests under pokemon-safari-app/src/services/pokeapi/ and helpers in src/test/pokeapi-test-helpers.ts per 02-VALIDATION.md Wave 0 list and RESEARCH Vitest example. Import intended production symbols from @/services/pokeapi/{keys,client,cache} and @/types/pokemon even if modules do not exist yet — tests must fail (RED) until plan 02-02. Cover DATA-01, DATA-02, DATA-04, D-03, D-06, D-10, D-11. Do not implement production cache/client yet. Do not install new npm packages. Do not call localStorage.clear() from any file that will become production code; if a test uses clear for isolation, confine it to *.test.ts only and prefer removeItem(CACHE_KEY).
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm test -- --run src/services/pokeapi 2>&1 | tee /tmp/p2-w0-svc.txt; test -f src/services/pokeapi/cache.test.ts && test -f src/services/pokeapi/client.test.ts && test -f src/services/pokeapi/keys.test.ts && test -f src/test/pokeapi-test-helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four files listed in files exist
    - npm test -- --run src/services/pokeapi exits non-zero OR reports failing/unresolved imports (RED)
    - keys.test.ts mentions pokemon-safari:poke-cache and pokemon-safari:save
    - cache.test.ts mentions getPokemon and QuotaExceeded or quota
  </acceptance_criteria>
  <done>Wave 0 service tests and helpers exist and are RED pending 02-02 implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 Boot, Game gate, and PokemonSprite tests</name>
  <files>pokemon-safari-app/src/screens/BootScreen.test.tsx, pokemon-safari-app/src/screens/GameScreen.test.tsx, pokemon-safari-app/src/components/PokemonSprite.test.tsx</files>
  <read_first>.planning/phases/02-pok-mon-data-layer/02-UI-SPEC.md, .planning/phases/02-pok-mon-data-layer/02-VALIDATION.md, pokemon-safari-app/src/App.test.tsx, pokemon-safari-app/src/components/BottomNav.test.tsx</read_first>
  <behavior>
    - BootScreen.test: loading shows Catching them all… with n/151 and role=progressbar named Catching them all (BOOT-04, D-04); on error shows Couldn’t catch the Pokédex data and Try again; clicking Try again does not call location.reload and resumes fetch (D-05).
    - GameScreen.test: when cache not ready, shows Safari is still packing… and See progress link/button toward /boot (D-02); Home/Settings remain outside this gate (gate is Game-only).
    - PokemonSprite.test: img has class pixelated; shiny=true uses front_shiny URL (D-07/D-09); onError or null src shows role=img placeholder same size (D-08).
  </behavior>
  <action>
    Write failing component tests matching 02-UI-SPEC copywriting exactly (straight apostrophe forms as in UI-SPEC: Couldn’t, Catching them all…). Wrap router-dependent screens with MemoryRouter basename="/pokemon-safari" like BottomNav.test.tsx. Import BootScreen, GameScreen, PokemonSprite from their eventual paths. Mock usePokemonCache / cache readiness as needed so assertions document the contract even before hooks exist. Do not implement BootScreen/PokemonSprite production components yet. Do not change App.tsx routing yet.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && test -f src/screens/BootScreen.test.tsx && test -f src/screens/GameScreen.test.tsx && test -f src/components/PokemonSprite.test.tsx && npm test -- --run src/screens/BootScreen.test.tsx src/screens/GameScreen.test.tsx src/components/PokemonSprite.test.tsx 2>&1 | tee /tmp/p2-w0-ui.txt; true</automated>
  </verify>
  <acceptance_criteria>
    - BootScreen.test.tsx, GameScreen.test.tsx, PokemonSprite.test.tsx exist
    - BootScreen.test mentions Try again and progressbar or Catching them all
    - GameScreen.test mentions Safari is still packing
    - PokemonSprite.test mentions pixelated
  </acceptance_criteria>
  <done>Wave 0 UI tests exist and are RED pending plans 02-03 and 02-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → localStorage / fetch mocks | Synthetic data only; no live PokéAPI required for Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Test helpers teaching clear() | mitigate | Helpers clear/remove only CACHE_KEY; document that production must never call localStorage.clear() (D-11); assert in keys/cache tests |
| T-02-SC | Tampering | npm installs | accept | No new packages in this plan; legitimacy N/A |
</threat_model>

<verification>
- Wave 0 files from 02-VALIDATION.md exist under pokemon-safari-app/src/
- Service and UI Wave 0 suites are RED (fail) until later plans
</verification>

<success_criteria>
- Nyquist Wave 0 gaps for Phase 2 are closed as failing tests
- Contracts encode BOOT-04, DATA-01, DATA-02, DATA-04 and D-01..D-11 behaviors for executors
</success_criteria>

<output>
Create `.planning/phases/02-pok-mon-data-layer/02-01-SUMMARY.md` when done
</output>
