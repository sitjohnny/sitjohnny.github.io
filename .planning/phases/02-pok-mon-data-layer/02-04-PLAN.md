---
phase: 02-pok-mon-data-layer
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - pokemon-safari-app/src/components/PokemonSprite.tsx
  - pokemon-safari-app/src/components/PokemonSprite.test.tsx
  - pokemon-safari-app/src/components/QuotaNote.tsx
  - pokemon-safari-app/src/screens/BootScreen.tsx
  - pokemon-safari-app/src/screens/GameScreen.tsx
  - pokemon-safari-app/src/hooks/usePokemonCache.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - BOOT-04
must_haves:
  truths:
    - "PokemonSprite renders front_default crisply with .pixelated and supports shiny front_shiny"
    - "Broken/missing sprites show a same-size silhouette placeholder without breaking layout"
    - "QuotaExceeded shows a non-blocking note; explore remains allowed from in-memory cache"
    - "When cache ready, Game can display at least one cached species via getPokemon + PokemonSprite with no PokéAPI calls"
  artifacts:
    - path: "pokemon-safari-app/src/components/PokemonSprite.tsx"
      provides: "pixelated sprite + shiny + silhouette (D-07/D-08/D-09)"
      exports: ["PokemonSprite"]
    - path: "pokemon-safari-app/src/components/QuotaNote.tsx"
      provides: "Non-blocking quota soft note (D-06)"
    - path: "pokemon-safari-app/src/screens/GameScreen.tsx"
      provides: "Ready-state sample sprite from getPokemon (DATA-02 proof)"
  key_links:
    - from: "pokemon-safari-app/src/components/PokemonSprite.tsx"
      to: "PokemonDto.sprites"
      via: "shiny prop selects front_shiny else front_default"
      pattern: "front_shiny|front_default|pixelated"
    - from: "pokemon-safari-app/src/screens/BootScreen.tsx"
      to: "QuotaNote"
      via: "status quota or persistCache quota result"
      pattern: "QuotaNote|Couldn’t save Pokémon data"
    - from: "pokemon-safari-app/src/screens/GameScreen.tsx"
      to: "getPokemon"
      via: "sync read only — no client fetch import"
      pattern: "getPokemon"
---

## Phase Goal

**As a** player, **I want to** prefetch Gen 1 Pokémon data once into a versioned local cache behind a Boot screen, **so that** gameplay never touches the network.

<objective>
Ship PokemonSprite (pixel + shiny + silhouette), QuotaNote for D-06 soft persistence failure, and a ready-state Game sample that proves sync cache reads without network (DATA-01 sprites, DATA-02, BOOT-04 quota path).

Purpose: Close Phase 2 visual/data contracts — crisp Gen 1 art path for later encounter/dex phases; never crash on shared-origin quota.
Output: PokemonSprite, QuotaNote, Game ready preview; PokemonSprite tests green; full suite green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-pok-mon-data-layer/02-UI-SPEC.md
@.planning/phases/02-pok-mon-data-layer/02-CONTEXT.md
@.planning/phases/02-pok-mon-data-layer/02-RESEARCH.md
@.planning/phases/02-pok-mon-data-layer/02-PATTERNS.md
@.planning/phases/02-pok-mon-data-layer/02-03-SUMMARY.md
@pokemon-safari-app/src/index.css
@pokemon-safari-app/src/components/EmptyState.tsx
@pokemon-safari-app/src/components/PixelButton.tsx
@pokemon-safari-app/src/types/pokemon.ts
@pokemon-safari-app/src/services/pokeapi/cache.ts

<interfaces>
PokemonDto.sprites: { front_default: string | null; front_shiny: string | null }
PokemonSprite props: { pokemon: PokemonDto; shiny?: boolean; size?: 64 | 96; alt: string }
Quota soft note copy (UI-SPEC): Couldn’t save Pokémon data on this device. You can still play this visit.
Dismiss: Got it
persistCache already returns 'ok' | 'quota' from plan 02-02
</interfaces>
</context>

## Artifacts this phase produces

- `PokemonSprite` — `src/components/PokemonSprite.tsx` (D-07, D-08, D-09)
- `QuotaNote` — `src/components/QuotaNote.tsx` (D-06)
- GameScreen ready branch: sample `getPokemon(25)` (or first available) + `PokemonSprite` proof of DATA-02
- BootScreen / hook wiring for quota status → QuotaNote

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PokemonSprite pixelated, shiny, silhouette fallback</name>
  <files>pokemon-safari-app/src/components/PokemonSprite.tsx, pokemon-safari-app/src/components/PokemonSprite.test.tsx</files>
  <read_first>.planning/phases/02-pok-mon-data-layer/02-UI-SPEC.md, .planning/phases/02-pok-mon-data-layer/02-RESEARCH.md, .planning/phases/02-pok-mon-data-layer/02-PATTERNS.md, pokemon-safari-app/src/index.css, pokemon-safari-app/src/components/PokemonSprite.test.tsx, pokemon-safari-app/src/types/pokemon.ts</read_first>
  <behavior>
    - Default art uses front_default URL (D-07); shiny true uses front_shiny (D-09).
    - img always has class pixelated; integer size default 96 allow 64; draggable false; required alt.
    - null src or img onError → same-size placeholder role=img aria-label=alt with muted/silhouette styling (D-08).
  </behavior>
  <action>
    Implement PokemonSprite per UI-SPEC §4 and RESEARCH PokemonSprite example. Use existing .pixelated utility — do not reimplement image-rendering elsewhere. No cards, no glow, no rounded-full. Placeholder fill: bg-muted or text-text at 22% opacity per UI-SPEC. Green PokemonSprite.test.tsx. Do not bundle PNGs into Vite; URLs only from DTO.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm test -- --run src/components/PokemonSprite.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PokemonSprite.tsx exports PokemonSprite and includes className pixelated
    - PokemonSprite.tsx handles shiny prop and onError or broken state
    - npm test -- --run src/components/PokemonSprite.test.tsx exits 0
  </acceptance_criteria>
  <done>Sprite primitive ready for encounter/dex phases with crisp scaling and shiny support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuotaNote + Game ready sample sprite</name>
  <files>pokemon-safari-app/src/components/QuotaNote.tsx, pokemon-safari-app/src/screens/BootScreen.tsx, pokemon-safari-app/src/screens/GameScreen.tsx, pokemon-safari-app/src/hooks/usePokemonCache.ts</files>
  <read_first>.planning/phases/02-pok-mon-data-layer/02-UI-SPEC.md, .planning/phases/02-pok-mon-data-layer/02-CONTEXT.md, pokemon-safari-app/src/screens/BootScreen.tsx, pokemon-safari-app/src/screens/GameScreen.tsx, pokemon-safari-app/src/hooks/usePokemonCache.ts, pokemon-safari-app/src/services/pokeapi/cache.ts</read_first>
  <behavior>
    - When persist returns quota / status quota: show QuotaNote with UI-SPEC copy; dismissible Got it; does not block navigation or Game unlock (D-06).
    - Game when ready: show placeholder adventure copy OR short ready confirmation plus PokemonSprite for getPokemon(25) (Pikachu) or id 1 — sync only (DATA-02).
    - Full automated suite green for Phase 2.
  </behavior>
  <action>
    Add QuotaNote presentational component (muted text, optional 4px accent left bar, Got it dismiss) per UI-SPEC §3 and D-06. Wire BootScreen (and/or first shell after ready) to show it when usePokemonCache reports quota — never crash, never require successful persist to unlock explore. On GameScreen ready branch, call getPokemon from cache service only (import getPokemon / isCacheReady from cache.ts — never client.ts) and render PokemonSprite with alt=species name; optional tiny shiny toggle is unnecessary — default shiny=false is enough; include one test or assert in GameScreen that client fetch module is unused. Run full npm test and fix regressions. Do not implement dex grid, encounters, or save writes. Do not call localStorage.clear().
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm test -- --run && npm run build && (rg -n "localStorage\\.clear" src/services src/screens src/hooks src/components || true) | rg -v "\\.test\\." | rg -v "^#" ; test -z "$(rg -n 'localStorage\.clear' src/services src/screens src/hooks src/components --glob '!**/*.test.*' || true)"</automated>
    <human-check>
      1. With warm cache, open Game: confirm a crisp pixelated sprite (not blurry) and DevTools Network filter pokeapi.co shows zero calls while on Game (DATA-02).
      2. Optionally simulate quota (fill localStorage) and confirm soft note appears without crash and Game remains usable after ready (D-06).
    </human-check>
  </verify>
  <acceptance_criteria>
    - QuotaNote.tsx contains Couldn’t save Pokémon data on this device
    - GameScreen.tsx imports getPokemon from cache path and renders PokemonSprite when ready
    - npm test -- --run exits 0
    - No localStorage.clear in production src/services|screens|hooks|components (tests excluded)
  </acceptance_criteria>
  <done>Phase 2 data layer UX complete: sprites, quota soft-fail, and sync Game preview with full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN sprite HTTPS URLs → img | Third-party image bytes; display only |
| localStorage quota → UX | Shared origin may reject writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | PokemonSprite src | mitigate | Only render URLs already validated into DTO (https); onError → placeholder |
| T-02-05 | Denial of Service | QuotaExceeded | mitigate | QuotaNote non-blocking; memory play allowed (D-06); explore not gated on persist |
| T-02-03 | Tampering | alt / aria-label | mitigate | React text props; no HTML injection |
| T-02-01 | Tampering | storage keys | mitigate | Still never localStorage.clear(); quota path does not purge SAVE_KEY |
| T-02-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- Full `npm test -- --run` green
- PokemonSprite + QuotaNote + Game sample wired
- Production code never calls localStorage.clear()
</verification>

<success_criteria>
- Crisp pixel sprites with shiny + silhouette (D-07/D-08/D-09)
- Quota soft note without crash (D-06)
- Game ready path proves DATA-02 sync cache read
- Phase 2 automated suite green for verify-work
</success_criteria>

<output>
Create `.planning/phases/02-pok-mon-data-layer/02-04-SUMMARY.md` when done
</output>
