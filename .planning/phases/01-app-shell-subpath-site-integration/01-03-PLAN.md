---
phase: 01-app-shell-subpath-site-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - index.html
  - README.md
  - pokemon-safari-app/package.json
  - pokemon-safari-app/scripts/assert-root-listing.mjs
  - pokemon-safari/
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
must_haves:
  truths:
    - "Root sitjohnny.github.io lists Food Crawl and Pokémon Safari with no sole meta-refresh to food-crawl"
    - "Published pokemon-safari/ contains production build with asset URLs under /pokemon-safari/"
    - "food-crawl/ is untouched by Safari deploy/copy"
  artifacts:
    - path: "index.html"
      provides: "Project listing for both apps"
      contains: "/pokemon-safari/"
    - path: "README.md"
      provides: "Projects table includes Pokémon Safari"
      contains: "pokemon-safari"
    - path: "pokemon-safari/index.html"
      provides: "Published SPA entry"
      contains: "/pokemon-safari/assets/"
    - path: "pokemon-safari-app/scripts/assert-root-listing.mjs"
      provides: "BOOT-02 automated assert"
  key_links:
    - from: "pokemon-safari-app/package.json"
      to: "pokemon-safari/"
      via: "deploy:copy copies dist into ../pokemon-safari"
      pattern: "deploy:copy"
    - from: "index.html"
      to: "/pokemon-safari/"
      via: "listing link (no http-equiv refresh sole redirect)"
      pattern: "href=\"/pokemon-safari/\""
---

## Phase Goal

**As a** visitor (child or parent), **I want to** open Pokémon Safari at `/pokemon-safari/` with a working mobile-first shell and find it listed on the site root, **so that** I can reach the game without broken assets or a food-crawl-only redirect.

<objective>
Integrate Pokémon Safari into the multi-project GitHub Pages site: replace the root auto-redirect with a dual-project listing, update README, and copy the Vite production build into published `pokemon-safari/` (D-05, D-13, BOOT-01/02).

Purpose: Complete the walking skeleton’s deploy path so Pages can serve the app beside food-crawl without SPA 404 hacks.
Output: Updated root `index.html` + README, `deploy:copy` script, committed `pokemon-safari/` artifacts, assert scripts green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-subpath-site-integration/01-CONTEXT.md
@.planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md
@.planning/phases/01-app-shell-subpath-site-integration/01-UI-SPEC.md
@index.html
@README.md
@food-crawl/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Root project listing and README (BOOT-02)</name>
  <files>index.html, README.md, pokemon-safari-app/scripts/assert-root-listing.mjs</files>
  <read_first>index.html, README.md, .planning/phases/01-app-shell-subpath-site-integration/01-UI-SPEC.md</read_first>
  <behavior>
    - assert-root-listing.mjs fails if index.html still has http-equiv refresh to /food-crawl/ as sole discovery path
    - assert-root-listing.mjs passes when both /food-crawl/ and /pokemon-safari/ links exist and heading includes sitjohnny projects
  </behavior>
  <action>
    Replace root `index.html` meta-refresh / canonical-only-to-food-crawl pattern with a static project listing per D-13 and UI-SPEC copy: heading sitjohnny projects; links Roosevelt Ave Food Crawl → `/food-crawl/` and Pokémon Safari → `/pokemon-safari/`; Safari blurb Catch ’em on a tiny Safari Zone.; Safari link color may use `#2F7A4B`. Remove sole auto-redirect that hides Safari. Update README.md Projects table to include `/pokemon-safari/` → Pokémon Safari alongside food-crawl. Add `pokemon-safari-app/scripts/assert-root-listing.mjs` that reads `../index.html` and enforces behavior above; wire optional npm script `test:root`. Do not modify files under `food-crawl/`. Do not add origin-root `404.html` spa redirect (D-14).
  </action>
  <verify>
    <automated>node pokemon-safari-app/scripts/assert-root-listing.mjs && rg -n "http-equiv=\"refresh\"" index.html | wc -l | awk '{exit $1==0?0:1}' && rg -n "pokemon-safari" README.md</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains href="/pokemon-safari/" and href="/food-crawl/"
    - index.html does not contain meta http-equiv refresh to food-crawl
    - README.md table lists Pokémon Safari
  </acceptance_criteria>
  <done>Root site and README discover both projects (BOOT-02).</done>
</task>

<task type="auto">
  <name>Task 2: Deploy copy published pokemon-safari/ artifacts (BOOT-01)</name>
  <files>pokemon-safari-app/package.json, pokemon-safari/</files>
  <read_first>.planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md, pokemon-safari-app/vite.config.ts</read_first>
  <action>
    Add npm script `deploy:copy` in pokemon-safari-app that removes/recreates `../pokemon-safari` and copies `dist/` contents into it (Pattern 4 — match food-crawl multi-folder publish; do NOT use gh-pages orphan branch). Run `npm run build && npm run deploy:copy`. Commit published static files under repo-root `pokemon-safari/` like food-crawl (RESEARCH open question recommendation). Verify built `pokemon-safari/index.html` references `/pokemon-safari/assets/`. Optionally add `pokemon-safari/404.html` copy of index only inside the Safari folder if desired for non-hash experiments — NOT required with HashRouter; do not add repo-root 404.html. Confirm `food-crawl/` unchanged (git status). Document in package README or script comment: local full-stack check is `npm run build && npm run deploy:copy && npx --yes serve .. -p 4173` or `vite preview` from app dir. Phase hard stop: no gameplay added during deploy.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm run build && npm run deploy:copy && node scripts/assert-build-base.mjs && rg -n "/pokemon-safari/assets/" ../pokemon-safari/index.html && test ! -f ../404.html && git status --short ../food-crawl | awk 'NF{exit 1}'; true'</automated>
    <human-check>
      1. After deploy:copy, open pokemon-safari/index.html via `vite preview` (base path) or a static server serving the repo root.
      2. Confirm `/pokemon-safari/` loads the shell and hash routes (#/game etc.) work on refresh.
      3. Confirm `/food-crawl/` still loads Roosevelt Ave Food Crawl.
      4. Confirm site root listing shows both projects.
    </human-check>
  </verify>
  <acceptance_criteria>
    - pokemon-safari/index.html exists and contains /pokemon-safari/assets/
    - package.json defines deploy:copy
    - No new root 404.html
    - food-crawl/ has no modifications from this plan
  </acceptance_criteria>
  <done>BOOT-01 published path ready; D-05 deploy copy complete; food-crawl safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Site root HTML → user navigation | Untrusted visitors follow listing links |
| Published pokemon-safari/ static files | Served as trusted-origin app code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | Root index listing links | mitigate | Hardcoded relative paths /food-crawl/ and /pokemon-safari/ only — no open redirect params |
| T-01-08 | Tampering | Root 404 / SPA fallback | mitigate | Do not add origin-root 404.html; HashRouter keeps food-crawl safe (D-14) |
| T-01-09 | Denial of Service | Overwriting food-crawl on deploy:copy | mitigate | deploy:copy targets only ../pokemon-safari; verify food-crawl untouched |
| T-01-SC | Tampering | npm installs | accept | No new runtime dependencies required for listing/copy scripts |
</threat_model>

<verification>
- assert-root-listing.mjs passes
- deploy:copy produces pokemon-safari/ with correct asset prefix
- food-crawl unmodified; no root 404.html
- Human: preview root listing + Safari hash routes + food-crawl regression
</verification>

<success_criteria>
- BOOT-02: dual project listing live at site root
- BOOT-01: published `/pokemon-safari/` build resolves assets
- Walking skeleton complete: scaffold → navigate → deployable publish folder
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-subpath-site-integration/01-03-SUMMARY.md` when done
</output>
