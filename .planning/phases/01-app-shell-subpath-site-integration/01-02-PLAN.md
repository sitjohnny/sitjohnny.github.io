---
phase: 01-app-shell-subpath-site-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pokemon-safari-app/src/index.css
  - pokemon-safari-app/src/App.tsx
  - pokemon-safari-app/src/main.tsx
  - pokemon-safari-app/src/components/AppShell.tsx
  - pokemon-safari-app/src/components/BottomNav.tsx
  - pokemon-safari-app/src/components/PixelButton.tsx
  - pokemon-safari-app/src/components/ScreenTitle.tsx
  - pokemon-safari-app/src/components/EmptyState.tsx
  - pokemon-safari-app/src/screens/HomeScreen.tsx
  - pokemon-safari-app/src/screens/GameScreen.tsx
  - pokemon-safari-app/src/screens/DexScreen.tsx
  - pokemon-safari-app/src/screens/PackScreen.tsx
  - pokemon-safari-app/src/screens/SettingsScreen.tsx
  - pokemon-safari-app/src/App.test.tsx
  - pokemon-safari-app/src/components/BottomNav.test.tsx
autonomous: true
requirements:
  - UX-01
  - BOOT-01
  - BOOT-03
must_haves:
  truths:
    - "Player can navigate Home, Game, Dex, Pack, and Settings via icon+label bottom chrome"
    - "Home shows brand Pokémon Safari, supporting sentence, and Start Safari CTA to Game"
    - "Placeholder screens show EmptyState copy; no map/encounter/capture gameplay"
    - "Hash routes refresh without asking the static server for nested paths"
  artifacts:
    - path: "pokemon-safari-app/src/components/BottomNav.tsx"
      provides: "5-route icon-first chrome with .touch-target"
      min_lines: 40
    - path: "pokemon-safari-app/src/components/AppShell.tsx"
      provides: "Outlet + BottomNav + optional top bar"
    - path: "pokemon-safari-app/src/screens/HomeScreen.tsx"
      provides: "Brand hero + Start Safari CTA"
    - path: "pokemon-safari-app/src/index.css"
      provides: "Emerald @theme tokens + .pixelated .pixel-border .touch-target"
      contains: "--color-dominant"
  key_links:
    - from: "pokemon-safari-app/src/App.tsx"
      to: "createHashRouter"
      via: "basename /pokemon-safari with child routes /, game, dex, pack, settings"
      pattern: "createHashRouter"
    - from: "pokemon-safari-app/src/screens/HomeScreen.tsx"
      to: "/game"
      via: "Start Safari navigation"
      pattern: "Start Safari"
    - from: "pokemon-safari-app/src/components/BottomNav.tsx"
      to: "NavLink routes"
      via: "Home Game Dex Pack Settings labels"
      pattern: "Pack"
---

## Phase Goal

**As a** visitor (child or parent), **I want to** open Pokémon Safari at `/pokemon-safari/` with a working mobile-first shell and find it listed on the site root, **so that** I can reach the game without broken assets or a food-crawl-only redirect.

<objective>
Ship the Emerald theme, pixel CSS utilities, HashRouter shell, and five placeholder screens with icon-first chrome so a child can tap between Home / Game / Dex / Pack / Settings (UX-01) without gameplay.

Purpose: Prove the walking-skeleton UI interaction end-to-end in-app (navigate + CTA) while locking BOOT-01 routing via createHashRouter (D-14) so deep links never break food-crawl.
Output: Complete AppShell + screens matching 01-UI-SPEC; Wave 0 App/BottomNav tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-subpath-site-integration/01-CONTEXT.md
@.planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md
@.planning/phases/01-app-shell-subpath-site-integration/01-UI-SPEC.md
@.planning/phases/01-app-shell-subpath-site-integration/01-SKELETON.md
@pokemon-safari-app/vite.config.ts
@pokemon-safari-app/src/store/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emerald theme and pixel-art CSS utilities</name>
  <files>pokemon-safari-app/src/index.css, pokemon-safari-app/index.html</files>
  <read_first>.planning/phases/01-app-shell-subpath-site-integration/01-UI-SPEC.md</read_first>
  <action>
    Expand `src/index.css` per D-09/D-10/D-11 and UI-SPEC: keep `@import "tailwindcss"`; add `@theme` tokens exactly — dominant `#E3F2C9`, secondary `#2F7A4B`, accent `#E8B923`, destructive `#C45C4A`, text `#1A3324`, on-secondary `#F5FBEF`, muted `#6B8F6E`, fonts Pixelify Sans + Fredoka. Add utilities `.pixelated` (image-rendering pixelated + crisp-edges), `.pixel-border` (4px secondary border, 4px radius, hard 4px offset shadow `#1A3324`), `.touch-target` (min 48×48). Load Google Fonts (display=swap) via index.html link or CSS import. Add reduced-motion rules for later CTA/nav animations. Forbidden: purple gradients, cream `#F4F1EA`+terracotta, dark-mode-default, glow, rounded-full nav pills, shadcn. No gameplay styles for D-pad/map.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && rg -n "color-dominant|--color-dominant|#E3F2C9" src/index.css && rg -n "\\.pixelated|\\.pixel-border|\\.touch-target" src/index.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css defines #E3F2C9, #2F7A4B, #E8B923 and .pixelated .pixel-border .touch-target
    - No purple-indigo theme tokens as primary palette
  </acceptance_criteria>
  <done>Global theme + pixel utilities match UI-SPEC contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HashRouter AppShell, BottomNav, and five placeholder screens</name>
  <files>pokemon-safari-app/src/App.tsx, pokemon-safari-app/src/main.tsx, pokemon-safari-app/src/components/AppShell.tsx, pokemon-safari-app/src/components/BottomNav.tsx, pokemon-safari-app/src/components/PixelButton.tsx, pokemon-safari-app/src/components/ScreenTitle.tsx, pokemon-safari-app/src/components/EmptyState.tsx, pokemon-safari-app/src/screens/HomeScreen.tsx, pokemon-safari-app/src/screens/GameScreen.tsx, pokemon-safari-app/src/screens/DexScreen.tsx, pokemon-safari-app/src/screens/PackScreen.tsx, pokemon-safari-app/src/screens/SettingsScreen.tsx, pokemon-safari-app/src/App.test.tsx, pokemon-safari-app/src/components/BottomNav.test.tsx</files>
  <read_first>.planning/phases/01-app-shell-subpath-site-integration/01-UI-SPEC.md, .planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md</read_first>
  <behavior>
    - BottomNav.test: five links Home, Game, Dex, Pack, Settings; each has visible text + icon; controls use touch-target (min 48px).
    - App.test: Home shows Pokémon Safari brand and Start Safari; activating Start Safari shows Game placeholder; BottomNav can reach Dex/Pack/Settings.
  </behavior>
  <action>
    Implement `createHashRouter` with `basename: '/pokemon-safari'` (D-04 + D-14 HashRouter — do NOT use BrowserRouter or root spa 404.html). Routes per D-12/UI-SPEC: `/` HomeScreen, `game` GameScreen, `dex` DexScreen, `pack` PackScreen (Inventory labeled Pack in chrome), `settings` SettingsScreen under AppShell with Outlet. BottomNav: always visible, secondary bar, safe-area padding, 5 NavLinks icon+label (never icon-only), accent active indicator, max-width 480px centered column on desktop. Home: Display brand Pokémon Safari, body Walk the grass. Meet Pokémon. Fill your Pokédex., PixelButton Start Safari → `/game`; meadow gradient atmosphere; omit top bar on Home. Placeholders: ScreenTitle + EmptyState with copy Safari isn’t ready yet / Coming in a later adventure.… Settings may include Reset Save stub dialog (Keep Progress / Erase Progress) with NO persist wiring. Inline SVG icons only (no Lucide). Motions: CTA press scale, nav accent slide, Home brand fade — honor prefers-reduced-motion. Hard stop D-15: no map movement, encounters, PokéAPI, inventory counts, or save writes. Green Wave 0 tests; optionally update lastRoute via Zustand stub on nav.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm test -- --run && npm run build</automated>
    <human-check>
      1. Run `npm run dev` in pokemon-safari-app, open the app URL Vite prints.
      2. Confirm Home brand + Start Safari, tap through all five bottom-nav destinations on a narrow viewport (~390px).
      3. Confirm no gameplay map — placeholders only.
    </human-check>
  </verify>
  <acceptance_criteria>
    - App.tsx uses createHashRouter (not createBrowserRouter)
    - BottomNav exposes Home, Game, Dex, Pack, Settings labels
    - npm test -- --run exits 0
    - No files under src/game/*.ts with movement/encounter logic beyond empty placeholders
  </acceptance_criteria>
  <done>User can navigate all five placeholder screens via icon-first chrome; HashRouter + basename locked; UX-01 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash / route → React tree | Untrusted path segments from location |
| User taps → navigation | Client-only; no server auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Route rendering | mitigate | Static route table only; React text escaping; no dangerouslySetInnerHTML; no URL-driven HTML injection |
| T-01-05 | Spoofing | Deep links / 404 strategy | mitigate | createHashRouter avoids origin-root 404.html that could hijack /food-crawl/ (D-14) |
| T-01-06 | Elevation of Privilege | Settings Reset Save stub | mitigate | Confirmation dialog required; no localStorage writes in Phase 1 (stub UI only) |
| T-01-SC | Tampering | npm installs | accept | No new packages in this plan beyond 01-01 lockfile |
</threat_model>

<verification>
- `npm test -- --run` green for App + BottomNav
- `npm run build` green
- Manual: five-tab nav + Start Safari on mobile-width viewport
</verification>

<success_criteria>
- UX-01 icon-first shell usable on phone-width and desktop column
- BOOT-01 client routing works with HashRouter + basename without nested-path server 404s
- D-12 placeholders only — no gameplay
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-subpath-site-integration/01-02-SUMMARY.md` when done
</output>
