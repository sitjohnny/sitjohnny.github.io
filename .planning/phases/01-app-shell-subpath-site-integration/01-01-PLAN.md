---
phase: 01-app-shell-subpath-site-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pokemon-safari-app/package.json
  - pokemon-safari-app/vite.config.ts
  - pokemon-safari-app/tsconfig.json
  - pokemon-safari-app/tsconfig.app.json
  - pokemon-safari-app/tsconfig.node.json
  - pokemon-safari-app/eslint.config.js
  - pokemon-safari-app/.prettierrc
  - pokemon-safari-app/index.html
  - pokemon-safari-app/src/main.tsx
  - pokemon-safari-app/src/App.tsx
  - pokemon-safari-app/src/index.css
  - pokemon-safari-app/src/vite-env.d.ts
  - pokemon-safari-app/src/store/index.ts
  - pokemon-safari-app/src/types/save.ts
  - pokemon-safari-app/src/test/setup.ts
  - pokemon-safari-app/src/App.test.tsx
  - pokemon-safari-app/src/components/BottomNav.test.tsx
  - pokemon-safari-app/scripts/assert-build-base.mjs
autonomous: true
requirements:
  - BOOT-03
  - BOOT-01
must_haves:
  truths:
    - "pokemon-safari-app builds with Vite base /pokemon-safari/ and strict TypeScript"
    - "Preferred src/ folders exist with Zustand stub and SaveEnvelope types"
    - "Vitest runs; Wave 0 route/nav tests exist (may fail until plan 01-02 greens UX assertions)"
  artifacts:
    - path: "pokemon-safari-app/vite.config.ts"
      provides: "base /pokemon-safari/, React+Tailwind plugins, @ alias, Vitest jsdom"
      contains: "base: '/pokemon-safari/'"
    - path: "pokemon-safari-app/src/types/save.ts"
      provides: "Versioned SaveEnvelope stub for Phase 7"
      exports: ["SaveEnvelope", "SaveEnvelopeV1"]
    - path: "pokemon-safari-app/src/store/index.ts"
      provides: "Zustand ui stub store (no persist)"
      contains: "create"
    - path: "pokemon-safari-app/src/App.test.tsx"
      provides: "Route/nav smoke tests for walking skeleton"
  key_links:
    - from: "pokemon-safari-app/vite.config.ts"
      to: "pokemon-safari-app/tsconfig.app.json"
      via: "@ path alias mirrored in compilerOptions.paths"
      pattern: '"@/\\*"'
    - from: "pokemon-safari-app/package.json"
      to: "pokemon-safari-app/vite.config.ts"
      via: "build script runs tsc -b && vite build"
      pattern: "tsc -b && vite build"
---

## Phase Goal

**As a** visitor (child or parent), **I want to** open Pokémon Safari at `/pokemon-safari/` with a working mobile-first shell and find it listed on the site root, **so that** I can reach the game without broken assets or a food-crawl-only redirect.

<objective>
Scaffold the Vite + React + TypeScript app under `pokemon-safari-app/`, lock tooling (Tailwind v4, Zustand stub, ESLint, Prettier, `@/` aliases, preferred folders, Vitest), and land Wave 0 failing/smoke tests for the walking skeleton.

Purpose: Establish the compile gate and test harness before shell UI and site publish so later plans stay thin and verifiable (BOOT-03; BOOT-01 base lock).
Output: Runnable `pokemon-safari-app/` with `npm run build` succeeding, folder layout, save envelope types, Zustand stub, and Vitest suite wired.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-subpath-site-integration/01-CONTEXT.md
@.planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md
@.planning/phases/01-app-shell-subpath-site-integration/01-UI-SPEC.md
@.planning/phases/01-app-shell-subpath-site-integration/01-SKELETON.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold app package and Wave 0 failing tests</name>
  <files>pokemon-safari-app/package.json, pokemon-safari-app/vite.config.ts, pokemon-safari-app/tsconfig.json, pokemon-safari-app/tsconfig.app.json, pokemon-safari-app/tsconfig.node.json, pokemon-safari-app/index.html, pokemon-safari-app/src/main.tsx, pokemon-safari-app/src/App.tsx, pokemon-safari-app/src/index.css, pokemon-safari-app/src/vite-env.d.ts, pokemon-safari-app/src/test/setup.ts, pokemon-safari-app/src/App.test.tsx, pokemon-safari-app/src/components/BottomNav.test.tsx, pokemon-safari-app/scripts/assert-build-base.mjs</files>
  <read_first>.planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md, .planning/phases/01-app-shell-subpath-site-integration/01-CONTEXT.md, .planning/research/SUMMARY.md</read_first>
  <behavior>
    - App.test: after HashRouter shell exists (plan 02), Start Safari / route smoke can find Home brand text Pokémon Safari and navigate toward Game; for Wave 0 write the assertions now so they fail until shell lands OR skip with describe.skip only if App cannot mount — prefer real failing asserts against temporary stub App that does not yet expose BottomNav labels.
    - BottomNav.test: expects five accessible links labeled Home, Game, Dex, Pack, Settings and touch-target class on controls (UX-01) — fail until BottomNav exists in plan 02.
    - assert-build-base.mjs: after `vite build`, dist/index.html must contain `/pokemon-safari/assets/` (BOOT-01).
  </behavior>
  <action>
    From repo root, create source app at `pokemon-safari-app/` via `npm create vite@latest pokemon-safari-app -- --template react-ts` (per D-01). Install locked stack versions from RESEARCH Standard Stack: react/react-dom ^19.2.8, react-router-dom ^7.18.1, zustand ^5.0.14; dev: vite ^8.1.5, @vitejs/plugin-react ^6.0.4, typescript ^7.0.2 (fallback pin typescript@5.9.3 only if toolchain breaks — Assumption A5), tailwindcss ^4.3.3, @tailwindcss/vite ^4.3.3, vitest ^4.1.10, jsdom ^29.1.1, @testing-library/react ^16.3.2, @testing-library/jest-dom ^7.0.0, @testing-library/user-event ^14.6.1, eslint ^10.8.0, typescript-eslint ^8.65.0, prettier ^3.9.6, eslint-config-prettier (verify package name on npm before lock — A1). Do not install howler, shadcn, or gameplay libs. Configure `vite.config.ts`: `base: '/pokemon-safari/'` (D-01/D-04), plugins react + `@tailwindcss/vite` (D-02), `resolve.alias` `@` → `./src` (D-07), `test.environment: 'jsdom'` and `setupFiles: './src/test/setup.ts'`. Mirror `@/*` → `./src/*` in `tsconfig.app.json`. Set `index.html` document title to Pokémon Safari per UI-SPEC. Create Wave 0 tests and `scripts/assert-build-base.mjs` as specified in behavior. Package legitimacy SUS rows are approved false-positives in RESEARCH — proceed without blocking human package gate. Do not create published `pokemon-safari/` yet (plan 03). Do not implement gameplay.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm test -- --run 2>&1 | tee /tmp/ps-wave0.txt; test -f src/App.test.tsx && test -f src/components/BottomNav.test.tsx && test -f scripts/assert-build-base.mjs</automated>
  </verify>
  <acceptance_criteria>
    - pokemon-safari-app/package.json exists and lists react, react-router-dom, zustand, vite, @tailwindcss/vite, vitest
    - pokemon-safari-app/vite.config.ts contains base: '/pokemon-safari/'
    - Wave 0 test files exist under src/
  </acceptance_criteria>
  <done>Source app exists with Vite base locked, Vitest configured, and Wave 0 test files present (nav/route tests may still fail until plan 01-02).</done>
</task>

<task type="auto">
  <name>Task 2: Folders, lint/format, Zustand stub, save envelope types</name>
  <files>pokemon-safari-app/eslint.config.js, pokemon-safari-app/.prettierrc, pokemon-safari-app/src/assets/.gitkeep, pokemon-safari-app/src/components/.gitkeep, pokemon-safari-app/src/screens/.gitkeep, pokemon-safari-app/src/hooks/.gitkeep, pokemon-safari-app/src/services/.gitkeep, pokemon-safari-app/src/game/.gitkeep, pokemon-safari-app/src/data/.gitkeep, pokemon-safari-app/src/store/index.ts, pokemon-safari-app/src/types/save.ts, pokemon-safari-app/src/utils/.gitkeep, pokemon-safari-app/package.json</files>
  <read_first>.planning/phases/01-app-shell-subpath-site-integration/01-RESEARCH.md, .planning/ROADMAP.md</read_first>
  <action>
    Create preferred empty directories per D-08: `src/{assets,components,screens,hooks,services,game,data,store,types,utils}/` (gitkeep or real files). Add ESLint flat config + Prettier + eslint-config-prettier per D-06 (Claude discretion on exact flat-config shape). Implement `src/store/index.ts` Zustand stub only (e.g. lastRoute + setLastRoute or settings.mute default false) — no `persist` middleware (D-03; persist is Phase 7). Implement `src/types/save.ts` with `SaveEnvelopeV1` (`version: 1`, `savedAt: string`, empty `data`) and `SaveEnvelope` union/alias (roadmap: types in Phase 1). Ensure `src/index.css` has `@import "tailwindcss"` (full Emerald `@theme` lands in plan 02). Wire package scripts: `dev`, `build` (`tsc -b && vite build`), `preview`, `test` (`vitest run`), `lint`, `format`. Run build once — must succeed (D-15). Do not add map/encounter/PokéAPI code under `game/` or `services/`.
  </action>
  <verify>
    <automated>cd pokemon-safari-app && npm run build && node scripts/assert-build-base.mjs && test -f src/types/save.ts && test -f src/store/index.ts && test -d src/game && test -d src/data && test -d src/services</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - dist/index.html contains /pokemon-safari/assets/
    - src/types/save.ts exports SaveEnvelopeV1 with version: 1
    - No persist( middleware in src/store/index.ts
  </acceptance_criteria>
  <done>BOOT-03 scaffold compiles; folders, lint/format, Zustand stub, and SaveEnvelope types are in place; build asset prefix asserts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | Package install supply chain |
| Browser → static assets under /pokemon-safari/ | Untrusted network loads built JS/CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm installs in pokemon-safari-app | mitigate | Install only packages listed in RESEARCH legitimacy table; commit lockfile; no postinstall scripts from unknown pkgs |
| T-01-02 | Tampering | src/store + future localStorage | mitigate | Do not call localStorage.clear(); no persist yet; document namespaced keys for later phases |
| T-01-03 | Information Disclosure | save types stub | accept | No PII; empty envelope; frontend-only toy game |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH Package Legitimacy Audit completed; SUS=too-new false positives on canonical packages — lockfile + approved list only |
</threat_model>

<verification>
- `cd pokemon-safari-app && npm run build` succeeds
- `node scripts/assert-build-base.mjs` confirms `/pokemon-safari/assets/` in dist
- Preferred directories exist; `src/types/save.ts` and `src/store/index.ts` exist
- Wave 0 test files present; BottomNav/App UX asserts may fail until 01-02
</verification>

<success_criteria>
- BOOT-03 tooling + folder layout + strict build green
- Vite `base: '/pokemon-safari/'` locked for BOOT-01
- Vitest + Wave 0 scaffolds ready for plan 01-02 to green UX-01
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-subpath-site-integration/01-01-SUMMARY.md` when done
</output>
