---
phase: 05-capture-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pokemon-safari-app/src/game/capture.test.ts
  - pokemon-safari-app/src/game/timing.test.ts
  - pokemon-safari-app/src/game/capture.ts
  - pokemon-safari-app/src/game/timing.ts
  - pokemon-safari-app/src/data/timingBar.ts
  - pokemon-safari-app/src/data/captureModifiers.ts
  - pokemon-safari-app/src/data/rates.ts
  - pokemon-safari-app/src/data/config-surface.test.ts
  - .planning/phases/05-capture-flow/05-VALIDATION.md
autonomous: true
requirements: [CATCH-02, CATCH-03, CATCH-05]

must_haves:
  truths:
    - "Catch chance is rarityBase + educationBonus + timing + ball + berry, then clamped to [0, 1] (D-22)"
    - "Miss is a timing modifier that still rolls — never an auto-fail (D-07)"
    - "Phase 5 defaults are ball poke and berry false with config slots retained (D-23)"
    - "Equal timing grade yields equal chance across attempt indexes — no pity (D-25)"
    - "Commons catch at a high rate and legendaries stay hard across seeded distribution tests (CATCH-05)"
    - "gradeAt maps a frozen position around an off-center sweet spot to perfect|great|good|miss; legendary zones are narrower than common (D-02, D-08, D-10)"
    - "sweetSpotFor(attempt) changes by attempt and stays inside config min/max (D-12)"
  artifacts:
    - path: "pokemon-safari-app/src/game/capture.ts"
      provides: "Pure additive catch chance + seeded roll"
      exports: ["computeCatchChance", "rollCapture", "CatchInputs"]
      min_lines: 40
    - path: "pokemon-safari-app/src/game/timing.ts"
      provides: "Pure timing grade + attempt-varying sweet spot"
      exports: ["TimingGrade", "gradeAt", "sweetSpotFor", "pingPong"]
      min_lines: 40
    - path: "pokemon-safari-app/src/data/timingBar.ts"
      provides: "Per-rarity zone widths, period, reduced-motion scale, sweet-spot config"
      contains: "timingBar"
    - path: "pokemon-safari-app/src/data/captureModifiers.ts"
      provides: "timing modifier slot beside rarity/ball/berry/education"
      contains: "timing"
    - path: "pokemon-safari-app/src/game/capture.test.ts"
      provides: "CATCH-03 / CATCH-05 / D-25 coverage"
    - path: "pokemon-safari-app/src/game/timing.test.ts"
      provides: "CATCH-02 grading + sweet-spot coverage"
  key_links:
    - from: "pokemon-safari-app/src/game/capture.ts"
      to: "pokemon-safari-app/src/data/captureModifiers.ts"
      via: "computeCatchChance reads cfg.rarity/timing/ball/berry"
      pattern: "captureModifiers"
    - from: "pokemon-safari-app/src/game/timing.ts"
      to: "pokemon-safari-app/src/data/timingBar.ts"
      via: "gradeAt / sweetSpotFor read timingBar zones and offsets"
      pattern: "timingBar"
    - from: "pokemon-safari-app/src/game/capture.ts"
      to: "pokemon-safari-app/src/utils/rng.ts"
      via: "rollCapture(rng, chance) uses injected Rng only"
      pattern: "Rng"
---

## Phase Goal

**As a** child who finished the math question, **I want to** throw with a timing bar and catch the Pokémon (or see it flee kindly after three tries), **so that** commons feel easy to catch and legendaries stay exciting.

> Source note: `ROADMAP.md` states the Phase 5 goal in outcome form rather than user-story form. The story above is a direct rendering of that goal plus success criteria and CONTEXT D-01…D-33; it introduces no new scope.

**This plan's slice:** Wave 0 failing tests plus the pure capture/timing math and `data/` tables behind them (CATCH-03, CATCH-02 grading, CATCH-05). Nothing is on screen yet; `05-02` mounts the first catchable path.

<objective>
Pin CATCH-03 / CATCH-02 / CATCH-05 contracts with failing Vitest suites, then implement `data/timingBar.ts`, extend `captureModifiers` + `encounterTimingMs`, and ship pure `game/capture.ts` + `game/timing.ts` so every later UI plan consumes real odds and grades (D-02, D-03, D-07, D-08, D-10, D-12, D-22, D-23, D-25).

Purpose: Capture odds and timing grades must be unit-testable before React mounts; DATA-03 forbids rate literals in components.
Output: timingBar config, timing modifiers, capture + timing pure modules, green unit suites, updated VALIDATION checklist rows for Wave 0.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-capture-flow/05-CONTEXT.md
@.planning/phases/05-capture-flow/05-RESEARCH.md
@.planning/phases/05-capture-flow/05-PATTERNS.md
@.planning/phases/05-capture-flow/05-UI-SPEC.md
@.planning/phases/05-capture-flow/05-VALIDATION.md
@pokemon-safari-app/src/data/captureModifiers.ts
@pokemon-safari-app/src/data/rates.ts
@pokemon-safari-app/src/data/config-surface.test.ts
@pokemon-safari-app/src/game/encounter.ts
@pokemon-safari-app/src/game/encounter.test.ts
@pokemon-safari-app/src/utils/rng.ts

<interfaces>
<!-- Contracts this plan ADDS -->

@/data/timingBar (new)
```typescript
export const timingBar = {
  periodMs: 1400,
  reducedMotionScale: 1.75,
  zones: {
    common: { perfect: 0.14, great: 0.26, good: 0.40 },
    rare: { perfect: 0.09, great: 0.18, good: 0.30 },
    legendary: { perfect: 0.05, great: 0.11, good: 0.20 },
  },
  sweetSpotOffsets: [0.62, 0.38, 0.70],
  sweetSpotJitter: 0.06,
  sweetSpotMin: 0.15,
  sweetSpotMax: 0.85,
} as const
```

@/data/captureModifiers (extend)
```typescript
timing: { perfect: 0.25, great: 0.15, good: 0.05, miss: -0.05 }
```

@/data/rates encounterTimingMs (extend)
```typescript
gradeFlash: 600, reducedGradeFlash: 300,
failBeat: 800, reducedFailBeat: 400,
shakeOnce: 180, reducedShakeOnce: 120,
shakeGap: 120, reducedShakeGap: 80,
shakeResolve: 400, reducedShakeResolve: 200,
```

@/game/capture (new)
```typescript
export type CatchInputs = {
  rarity: RarityBand
  educationBonus: number
  grade: TimingGrade
  ball?: 'poke' | 'great'
  berry?: boolean
}
export function computeCatchChance(inputs: CatchInputs, cfg?): number
export function rollCapture(rng: Rng, chance: number): boolean
```

@/game/timing (new)
```typescript
export type TimingGrade = 'perfect' | 'great' | 'good' | 'miss'
export function gradeAt(position, sweetSpot, rarity, cfg?): TimingGrade
export function sweetSpotFor(attempt, cfg?, rng?): number
export function pingPong(elapsedMs: number, periodMs: number): number
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing capture, timing, and config-surface tests</name>
  <files>pokemon-safari-app/src/game/capture.test.ts, pokemon-safari-app/src/game/timing.test.ts, pokemon-safari-app/src/data/config-surface.test.ts, .planning/phases/05-capture-flow/05-VALIDATION.md</files>
  <read_first>
    - pokemon-safari-app/src/game/encounter.test.ts (stubRng + seeded distribution + purity pattern)
    - pokemon-safari-app/src/data/config-surface.test.ts (export + Math.random / rate-literal gates)
    - pokemon-safari-app/src/data/captureModifiers.ts (current shape before timing slot)
    - pokemon-safari-app/src/data/rates.ts (encounterTimingMs keys to extend)
    - .planning/phases/05-capture-flow/05-RESEARCH.md (Validation Architecture + Code Examples numbers)
    - .planning/phases/05-capture-flow/05-VALIDATION.md (Wave 0 checklist)
    - .planning/phases/05-capture-flow/05-CONTEXT.md (D-07, D-22, D-23, D-25)
  </read_first>
  <behavior>
    - computeCatchChance sums rarity + education + timing + ball + berry then clamps [0,1] (D-22)
    - Miss grade still yields a finite chance and rolls (D-07); default ball poke / berry false (D-23)
    - Equal grade ⇒ equal chance for attempt 0 vs 2 (D-25 — no attempt index in formula)
    - rollCapture: rng.next() &lt; chance catches; &gt;= fails; never touches localStorage
    - CATCH-05: two seeds × 10000 rolls — common+correct+good catch rate ≥ 0.70; legendary+incorrect+miss ≤ 0.20
    - gradeAt boundary probes around off-center sweet spot; legendary perfect half-width &lt; common
    - sweetSpotFor(0), (1), (2) are not all equal and stay in [sweetSpotMin, sweetSpotMax]
    - config-surface asserts captureModifiers.timing keys + timingBar export + new encounterTimingMs keys
  </behavior>
  <action>
Create Wave 0 failing suites that import final module paths so RED is missing modules / failing assertions.

1. `src/game/capture.test.ts` — stubRng helper mirroring encounter.test.ts. Cover computeCatchChance additive terms using imported captureModifiers (never restating rarity bases as bare literals in expectations beyond reading cfg); clamp above 1 and below 0; NaN-safe clamp to 0; default ball poke and berry false (D-23); Miss still returns a number and rollCapture can still catch when chance &gt; stub (D-07); D-25: same inputs except documenting attempt is irrelevant — call twice with identical CatchInputs; rollCapture boundaries at chance-epsilon and chance; purity / no localStorage; CATCH-05 distribution describe using createRng(42) and createRng(99) at 10000 draws for (common, education 0.15, good) and (legendary, education 0, miss) against the thresholds above.

2. `src/game/timing.test.ts` — gradeAt probes at sweetSpot, sweetSpot±perfect, ±great, ±good, and far Miss for common and legendary; assert legendary zones.perfect &lt; common zones.perfect via imported timingBar; sweetSpotFor varies by attempt; pingPong(0,period)=0, pingPong(period/4)=0.5, pingPong(period/2)=1, pingPong(3period/4)=0.5.

3. Extend `config-surface.test.ts` — import timingBar; assert timing keys perfect/great/good/miss on captureModifiers; assert timingBar.periodMs, reducedMotionScale, zones.common|rare|legendary, sweetSpotOffsets length ≥ 3; extend encounterTimingMs key list with gradeFlash, reducedGradeFlash, failBeat, reducedFailBeat, shakeOnce, reducedShakeOnce, shakeGap, reducedShakeGap, shakeResolve, reducedShakeResolve.

4. Mark Wave 0 rows in 05-VALIDATION.md for capture/timing/config as pending→in progress notes only if the checklist items exist; do not claim green until Task 2.
  </action>
  <acceptance_criteria>
    - `cd pokemon-safari-app && ! npx vitest run src/game/capture.test.ts src/game/timing.test.ts` exits non-zero (intentional RED)
    - `grep -c "computeCatchChance" pokemon-safari-app/src/game/capture.test.ts` is ≥ 3
    - `grep -c "gradeAt" pokemon-safari-app/src/game/timing.test.ts` is ≥ 2
    - `grep -c "timingBar" pokemon-safari-app/src/data/config-surface.test.ts` is ≥ 1
    - `grep -c "captureModifiers.timing\|timing:" pokemon-safari-app/src/data/config-surface.test.ts` is ≥ 1
    - No production `src/game/capture.ts` or `src/game/timing.ts` implementation yet (or tests still fail if stubs empty)
  </acceptance_criteria>
  <verify>
    <automated>cd pokemon-safari-app && ! npx vitest run src/game/capture.test.ts src/game/timing.test.ts</automated>
  </verify>
  <done>Wave 0 capture/timing/config contracts fail for the right reasons before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement capture/timing config and pure game modules</name>
  <files>pokemon-safari-app/src/data/timingBar.ts, pokemon-safari-app/src/data/captureModifiers.ts, pokemon-safari-app/src/data/rates.ts, pokemon-safari-app/src/game/capture.ts, pokemon-safari-app/src/game/timing.ts</files>
  <read_first>
    - pokemon-safari-app/src/game/capture.test.ts
    - pokemon-safari-app/src/game/timing.test.ts
    - pokemon-safari-app/src/data/config-surface.test.ts
    - pokemon-safari-app/src/game/encounter.ts (purity header + injected Rng)
    - pokemon-safari-app/src/data/captureModifiers.ts
    - pokemon-safari-app/src/data/rates.ts
    - .planning/phases/05-capture-flow/05-RESEARCH.md (Patterns 1–2, Code Examples)
    - .planning/phases/05-capture-flow/05-CONTEXT.md (D-02, D-07, D-08, D-10, D-12, D-22, D-23, D-25)
  </read_first>
  <behavior>
    - Same behaviors as Task 1; suites must go GREEN
  </behavior>
  <action>
Implement GREEN behind the Task 1 contracts.

1. Add `src/data/timingBar.ts` with the interface values from this plan (periodMs 1400, reducedMotionScale 1.75, per-rarity zone half-widths, sweetSpotOffsets [0.62, 0.38, 0.70], jitter 0.06, min 0.15, max 0.85) as const — Claude discretion locked here for CATCH-05 kid-friendliness (D-08, D-10, D-12).

2. Extend `captureModifiers` with `timing: { perfect: 0.25, great: 0.15, good: 0.05, miss: -0.05 }` (D-02, D-07). Keep rarity common 0.6 / rare 0.3 / legendary 0.1, ball poke 0 / great 0.15, berry 0.1, education from rates.

3. Extend `encounterTimingMs` with gradeFlash 600, reducedGradeFlash 300, failBeat 800, reducedFailBeat 400, shakeOnce 180, reducedShakeOnce 120, shakeGap 120, reducedShakeGap 80, shakeResolve 400, reducedShakeResolve 200 (UI-SPEC D10).

4. Implement `game/capture.ts`: computeCatchChance additive then clamp [0,1] (D-22); default ball poke and berry false (D-23); educationBonus comes from caller (session.captureBonus — D-03); rollCapture uses rng.next() &lt; chance only — never Math.random or getDefaultRng inside game/.

5. Implement `game/timing.ts`: TimingGrade union; gradeAt by distance from sweetSpot using cfg.zones[rarity] (D-08); sweetSpotFor rotates offsets by attempt with optional jitter clamped to min/max (D-10, D-12); export pingPong triangle wave for 05-03 indicator.

6. Do not create React components or change encounter stages in this plan.
  </action>
  <acceptance_criteria>
    - `cd pokemon-safari-app && npx vitest run src/game/capture.test.ts src/game/timing.test.ts src/data/config-surface.test.ts` exits 0
    - `grep -n "Math.random" pokemon-safari-app/src/game/capture.ts pokemon-safari-app/src/game/timing.ts` finds no matches outside comments (prefer zero matches)
    - `grep -c "export function computeCatchChance" pokemon-safari-app/src/game/capture.ts` equals 1
    - `grep -c "export function gradeAt" pokemon-safari-app/src/game/timing.ts` equals 1
    - `grep -c "timing:" pokemon-safari-app/src/data/captureModifiers.ts` is ≥ 1
    - `grep -c "export const timingBar" pokemon-safari-app/src/data/timingBar.ts` equals 1
    - Wave 0 checklist items for capture.test / timing.test / config-surface timingBar in 05-VALIDATION.md marked complete or status updated to green
  </acceptance_criteria>
  <verify>
    <automated>cd pokemon-safari-app && npx vitest run src/game/capture.test.ts src/game/timing.test.ts src/data/config-surface.test.ts</automated>
  </verify>
  <done>Pure capture and timing math are config-driven, seeded-testable, and green for CATCH-02/03/05 foundations.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | Path |
|--------|------|------|
| `timingBar` | const export | `src/data/timingBar.ts` |
| `captureModifiers.timing` | config field | `src/data/captureModifiers.ts` |
| `encounterTimingMs.gradeFlash` (+ reduced/fail/shake*) | config fields | `src/data/rates.ts` |
| `CatchInputs` | type | `src/game/capture.ts` |
| `computeCatchChance` | function | `src/game/capture.ts` |
| `rollCapture` | function | `src/game/capture.ts` |
| `TimingGrade` | type | `src/game/timing.ts` |
| `gradeAt` | function | `src/game/timing.ts` |
| `sweetSpotFor` | function | `src/game/timing.ts` |
| `pingPong` | function | `src/game/timing.ts` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/ → game/ | Config numbers cross into pure math; must stay in data/ (DATA-03) |
| game/ → tests | Injected Rng only; no ambient Math.random |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | capture odds / zone widths | mitigate | Keep all rates in `data/`; enforce via `config-surface.test.ts`; no literals in components |
| T-05-02 | Elevation | N/A (offline kid game) | accept | No privileged roles or server |
| T-05-03 | Information Disclosure | catch % UI | mitigate | Plan 05-02 forbids raw final % (D-24); this plan never exports UI % helpers for final chance |
| T-05-SC | Tampering | npm installs | accept | No package installs this phase — legitimacy N/A |
</threat_model>

<verification>
`npx vitest run src/game/capture.test.ts src/game/timing.test.ts src/data/config-surface.test.ts` green; no Math.random in new game modules.
</verification>

<success_criteria>
Additive clamped catch chance, seeded rolls, timing grades, and per-rarity zones exist in pure modules with CATCH-05 distribution gates passing.
</success_criteria>

<output>
Create `.planning/phases/05-capture-flow/05-01-SUMMARY.md` when done
</output>
