---
phase: 05-capture-flow
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - pokemon-safari-app/src/components/encounter/TimingBar.tsx
  - pokemon-safari-app/src/components/encounter/TimingBar.test.tsx
  - pokemon-safari-app/src/components/encounter/GradeFlash.tsx
  - pokemon-safari-app/src/index.css
  - pokemon-safari-app/src/hooks/useEncounterFlow.ts
autonomous: true
requirements: [CATCH-02]

must_haves:
  truths:
    - "Timing indicator ping-pongs continuously left↔right until Capture and starts moving immediately on mount (D-01, D-09, D-19)"
    - "Capture freezes the indicator; that frozen position is graded; bar shows color bands only until grade flash (D-11, D-20)"
    - "Rarity scales zone widths from timingBar.zones; indicator period stays constant except reduced-motion slows period × 1.75 (D-08, D-17)"
    - "Large Capture button under the bar; Space/Enter fire Capture; tapping the track does not (D-13)"
    - "Mashed Capture during grade flash / shake is ignored (D-21)"
    - "Composition is sprite + meta + bar + Capture only — no Run, no odds meter (D-18, D-27)"
  artifacts:
    - path: "pokemon-safari-app/src/components/encounter/TimingBar.tsx"
      provides: "rAF/time ping-pong indicator + Capture UX"
      contains: "Capture"
      min_lines: 80
    - path: "pokemon-safari-app/src/components/encounter/TimingBar.test.tsx"
      provides: "Capture lock, keyboard, band-only bar coverage"
    - path: "pokemon-safari-app/src/index.css"
      provides: "timing-track / timing-indicator / boost-chip utilities"
      contains: "timing-track"
  key_links:
    - from: "pokemon-safari-app/src/components/encounter/TimingBar.tsx"
      to: "pokemon-safari-app/src/game/timing.ts"
      via: "pingPong drives position ref; Capture reads ref into capture()"
      pattern: "pingPong"
    - from: "pokemon-safari-app/src/components/encounter/TimingBar.tsx"
      to: "pokemon-safari-app/src/data/timingBar.ts"
      via: "periodMs and zones for bands + motion"
      pattern: "timingBar"
    - from: "pokemon-safari-app/src/components/encounter/TimingBar.tsx"
      to: "pokemon-safari-app/src/hooks/useMapCamera.ts"
      via: "prefersReducedMotion scales period (D-17)"
      pattern: "prefersReducedMotion"
    - from: "pokemon-safari-app/src/components/encounter/GradeFlash.tsx"
      to: "pokemon-safari-app/src/components/encounter/BallShake.tsx"
      via: "AppearFlash-style timer; onComplete then Overlay mounts BallShake (D-14) — never concurrent"
      pattern: "onComplete"
---

## Phase Goal

**As a** child who finished the math question, **I want to** throw with a timing bar and catch the Pokémon (or see it flee kindly after three tries), **so that** commons feel easy to catch and legendaries stay exciting.

**This plan's slice:** make the timing mini-game feel real — continuous ping-pong, rarity-scaled color bands, Capture freeze + lock, keyboard, reduced-motion slower period (CATCH-02 full interaction).

<objective>
Upgrade TimingBar from the 05-02 happy-path shell into the UI-SPEC timing contract: imperative rAF/time position source of truth, color-only bands, Capture/Space/Enter, mash lock during flash/shake (D-01, D-08–D-14, D-17–D-21).

Purpose: Accuracy must visibly matter; commons get wider Perfect/Great/Good bands than legendaries.
Output: production TimingBar + TimingBar.test.tsx + CSS utilities; GradeFlash timing from config.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-capture-flow/05-CONTEXT.md
@.planning/phases/05-capture-flow/05-UI-SPEC.md
@.planning/phases/05-capture-flow/05-RESEARCH.md
@.planning/phases/05-capture-flow/05-PATTERNS.md
@.planning/phases/05-capture-flow/05-02-SUMMARY.md
@pokemon-safari-app/src/components/encounter/TimingBar.tsx
@pokemon-safari-app/src/components/encounter/EducationQuestion.test.tsx
@pokemon-safari-app/src/hooks/useExploreLoop.ts
@pokemon-safari-app/src/hooks/useMapCamera.ts
@pokemon-safari-app/src/data/timingBar.ts
@pokemon-safari-app/src/game/timing.ts
@pokemon-safari-app/src/test/setup.ts
@pokemon-safari-app/src/index.css

<interfaces>
@/game/timing — pingPong(elapsedMs, periodMs), gradeAt, TimingGrade
@/data/timingBar — periodMs, reducedMotionScale, zones
@/hooks/useMapCamera — prefersReducedMotion()
@/test/setup — flushFrames for rAF
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TimingBar interaction tests</name>
  <files>pokemon-safari-app/src/components/encounter/TimingBar.test.tsx</files>
  <read_first>
    - pokemon-safari-app/src/components/encounter/EducationQuestion.test.tsx (RTL + user-event patterns)
    - pokemon-safari-app/src/test/setup.ts (flushFrames / rAF shim)
    - pokemon-safari-app/src/components/encounter/TimingBar.tsx (current 05-02 shell)
    - .planning/phases/05-capture-flow/05-UI-SPEC.md (Timing Bar Interaction Spec, colors, a11y)
    - .planning/phases/05-capture-flow/05-CONTEXT.md (D-11, D-13, D-19, D-20, D-21)
    - .planning/phases/05-capture-flow/05-VALIDATION.md
  </read_first>
  <behavior>
    - Renders Capture button; accessible name mentions timing / Capture
    - Click Capture invokes onCapture/capture once with a numeric position in [0,1]
    - Space/Enter on focused Capture fires once
    - Clicking the track/bar does not call capture
    - When locked (prop or stage shake), Capture is disabled / clicks ignored
    - Bar DOM has no text Perfect/Great/Good/Miss labels (D-11) — grade flash is separate
    - Math boost and Throw n of 3 text present
  </behavior>
  <action>
Add TimingBar.test.tsx covering CATCH-02 interaction (D-11, D-13, D-21). Mount TimingBar with a mock session (common rarity, captureBonus 0.15, attemptsUsed 0, sweetSpot 0.62) and a spy capture handler. Use flushFrames if asserting motion side effects. Assert color-band elements or CSS classes exist without grade word labels inside the track. Assert aria text for the timing group per UI-SPEC.
  </action>
  <acceptance_criteria>
    - `cd pokemon-safari-app && ! npx vitest run src/components/encounter/TimingBar.test.tsx` exits non-zero until Task 2 greens it, OR file exists with failing asserts against incomplete shell
    - `grep -c "Capture" pokemon-safari-app/src/components/encounter/TimingBar.test.tsx` is ≥ 3
    - `grep -c "Throw" pokemon-safari-app/src/components/encounter/TimingBar.test.tsx` is ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>cd pokemon-safari-app && ! npx vitest run src/components/encounter/TimingBar.test.tsx</automated>
  </verify>
  <done>TimingBar interaction contracts exist as RED/incomplete against the shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ping-pong TimingBar and CSS bands</name>
  <files>pokemon-safari-app/src/components/encounter/TimingBar.tsx, pokemon-safari-app/src/components/encounter/GradeFlash.tsx, pokemon-safari-app/src/index.css, pokemon-safari-app/src/hooks/useEncounterFlow.ts</files>
  <read_first>
    - pokemon-safari-app/src/components/encounter/TimingBar.test.tsx
    - pokemon-safari-app/src/components/encounter/TimingBar.tsx
    - pokemon-safari-app/src/hooks/useExploreLoop.ts (rAF + refs, no per-frame React state)
    - pokemon-safari-app/src/hooks/useMapCamera.ts (prefersReducedMotion)
    - pokemon-safari-app/src/data/timingBar.ts
    - pokemon-safari-app/src/game/timing.ts
    - pokemon-safari-app/src/index.css
    - .planning/phases/05-capture-flow/05-UI-SPEC.md (band colors, spacing, motion, CSS utilities)
    - .planning/phases/05-capture-flow/05-CONTEXT.md (D-01, D-08–D-14, D-17–D-21)
  </read_first>
  <behavior>
    - Same as Task 1; suite green
  </behavior>
  <action>
Complete CATCH-02 timing UX (D-01, D-08–D-14, D-17–D-21):

1. TimingBar: on mount start rAF (or time-derived) loop writing posRef via pingPong(elapsed, period) immediately (D-19). period = timingBar.periodMs, or periodMs * reducedMotionScale when prefersReducedMotion() (D-17). Write indicator transform from the same posRef (single source of truth — D-20). Do not put position in React state each frame.

2. Render color bands only from timingBar.zones[session.rarity] around sweetSpot — Perfect #E8B923, Great #2F7A4B, Good color-mix secondary/dominant, Miss muted wash (UI-SPEC). No Perfect/Great/Good text on the bar (D-11).

3. Capture PixelButton full width ≥48px tall under bar; onClick cancels rAF, freezes visual at posRef, calls capture(posRef.current). Space/Enter via native button. Track onClick must not call capture (D-13).

4. While parent stage is not timing (or locked prop true during flash), disable Capture and ignore presses (D-21). GradeFlash — analog AppearFlash.tsx presentational timer (05-PATTERNS.md): Display 32px Perfect!/Great!/Good!/Miss! for gradeFlash/reducedGradeFlash ms then onComplete; Overlay must keep BallShake unmounted until that handoff (D-14). Do not start shake immediately on Capture.

5. Add .timing-track, .timing-indicator, .boost-chip CSS from UI-SPEC. Keep composition sprite + labels + bar + Capture only (D-18); no Run button (D-27).

6. Ensure flow still gates capture() on stage===timing.
  </action>
  <acceptance_criteria>
    - `cd pokemon-safari-app && npx vitest run src/components/encounter/TimingBar.test.tsx src/hooks/useEncounterFlow.test.ts` exits 0
    - `grep -c "pingPong\|requestAnimationFrame" pokemon-safari-app/src/components/encounter/TimingBar.tsx` is ≥ 1
    - `grep -c "prefersReducedMotion" pokemon-safari-app/src/components/encounter/TimingBar.tsx` is ≥ 1
    - `grep -c "timing-track" pokemon-safari-app/src/index.css` is ≥ 1
    - `grep -nE "Perfect!|Great!|Good!|Miss!" pokemon-safari-app/src/components/encounter/TimingBar.tsx` finds no grade flash words inside the track markup (words may exist only if mistakenly added — prefer 0 matches in TimingBar; flash belongs in GradeFlash)
    - `grep -c "Perfect!" pokemon-safari-app/src/components/encounter/GradeFlash.tsx` is ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>cd pokemon-safari-app && npx vitest run src/components/encounter/TimingBar.test.tsx src/hooks/useEncounterFlow.test.ts</automated>
  </verify>
  <done>Timing bar ping-pongs, freezes on Capture, rarity-scales bands, and locks input during resolution.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | Path |
|--------|------|------|
| `TimingBar` (rAF ping-pong) | component behavior | `src/components/encounter/TimingBar.tsx` |
| `TimingBar.test.tsx` | test module | `src/components/encounter/TimingBar.test.tsx` |
| `.timing-track` / `.timing-indicator` / `.boost-chip` | CSS utilities | `src/index.css` |
| `GradeFlash` copy Perfect!/Great!/Good!/Miss! | component | `src/components/encounter/GradeFlash.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM events → capture() | Only Capture button / keyboard; track taps ignored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | Capture mash | mitigate | Disabled button + stage===timing gate (D-21) |
| T-05-06 | Tampering | per-frame React state | mitigate | Imperative ref position only (MAP-04 precedent) |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
TimingBar tests green; ping-pong uses pingPong + prefersReducedMotion; no grade labels on the bar.
<human-check>
Confirm continuous indicator motion, slower under prefers-reduced-motion, Capture freezes marker, Space/Enter works, track tap does not capture.
</human-check>
</verification>

<success_criteria>
CATCH-02 timing mini-game matches UI-SPEC interaction: continuous bar, rarity zones, Capture freeze, mash lock, reduced-motion slower period.
</success_criteria>

<output>
Create `.planning/phases/05-capture-flow/05-03-SUMMARY.md` when done
</output>
