---
phase: 05-capture-flow
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - pokemon-safari-app/src/components/encounter/FleeCard.tsx
  - pokemon-safari-app/src/components/encounter/BallShake.tsx
  - pokemon-safari-app/src/components/encounter/EncounterOverlay.tsx
  - pokemon-safari-app/src/hooks/useEncounterFlow.ts
  - pokemon-safari-app/src/hooks/useEncounterFlow.test.ts
  - pokemon-safari-app/src/store/encounterStore.ts
  - pokemon-safari-app/src/data/educationConfig.ts
  - pokemon-safari-app/src/game/capture.test.ts
  - pokemon-safari-app/src/screens/GameScreen.test.tsx
  - .planning/phases/05-capture-flow/05-VALIDATION.md
autonomous: true
requirements: [CATCH-04, CATCH-05]

must_haves:
  truths:
    - "A failed throw with attempts remaining shows Oh! It broke free! then remounts timing with a new sweet spot — no Try Again gate (D-12, D-26)"
    - "After three failed capture attempts the player sees It got away! / That’s okay — you’ll find another! with Continue (D-04, D-28)"
    - "Caught Continue and Flee Continue both route to recap when education was wrong, else close to map (D-29)"
    - "Odds do not escalate across throws — only timing grade changes (D-25)"
    - "No Run or berry actions between throws (D-27)"
    - "CATCH-05 distribution gates remain green for commons vs legendaries"
  artifacts:
    - path: "pokemon-safari-app/src/components/encounter/FleeCard.tsx"
      provides: "Kind flee card"
      contains: "It got away!"
    - path: "pokemon-safari-app/src/hooks/useEncounterFlow.ts"
      provides: "Fail beat → retry timing; third fail → flee; continueFromFlee"
      exports: ["continueFromFlee", "onShakeComplete"]
    - path: "pokemon-safari-app/src/hooks/useEncounterFlow.test.ts"
      provides: "Three-fail flee + recap routing coverage"
  key_links:
    - from: "pokemon-safari-app/src/hooks/useEncounterFlow.ts"
      to: "pokemon-safari-app/src/store/encounterStore.ts"
      via: "on fail increment attemptsUsed; &lt;3 startTiming; else toFlee"
      pattern: "toFlee"
    - from: "pokemon-safari-app/src/components/encounter/FleeCard.tsx"
      to: "continueFromFlee"
      via: "Continue button"
      pattern: "continueFromFlee"
    - from: "pokemon-safari-app/src/hooks/useEncounterFlow.ts"
      to: "toRecap"
      via: "wrong education after result or flee Continue (D-29)"
      pattern: "toRecap"
---

## Phase Goal

**As a** child who finished the math question, **I want to** throw with a timing bar and catch the Pokémon (or see it flee kindly after three tries), **so that** commons feel easy to catch and legendaries stay exciting.

**This plan's slice:** complete the retry → flee loop with kind copy, preserve wrong-answer recap, and lock CATCH-05 distribution (CATCH-04, CATCH-05).

<objective>
Finish Phase 5 by wiring fail beat → remounted timing with repositioned sweet spot, flee after three fails (D-04, D-26, D-28), Continue → recap|map (D-29), and confirming CATCH-05 / D-25 tests stay green with no pity and no Run/berry UI (D-25, D-27).

Purpose: Without retry/flee the capture loop is incomplete; without kind copy it violates CATCH-04 / UX-02.
Output: FleeCard, fail-beat path, shake fail ending (ball opens), extended flow tests, VALIDATION nyquist sign-off updates.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-capture-flow/05-CONTEXT.md
@.planning/phases/05-capture-flow/05-UI-SPEC.md
@.planning/phases/05-capture-flow/05-RESEARCH.md
@.planning/phases/05-capture-flow/05-03-SUMMARY.md
@pokemon-safari-app/src/hooks/useEncounterFlow.ts
@pokemon-safari-app/src/store/encounterStore.ts
@pokemon-safari-app/src/components/encounter/EncounterOverlay.tsx
@pokemon-safari-app/src/components/encounter/BallShake.tsx
@pokemon-safari-app/src/components/encounter/CaughtCard.tsx
@pokemon-safari-app/src/components/encounter/RecapCard.tsx
@pokemon-safari-app/src/data/educationConfig.ts
@pokemon-safari-app/src/data/rates.ts
@pokemon-safari-app/src/game/capture.test.ts

<interfaces>
@/store — toFlee(), increment via registerThrow/onShakeComplete
@/flow
```typescript
export function onShakeComplete(): void
// if lastCaught → toResult
// else attemptsUsed += 1 (if not already); if attemptsUsed >= 3 → toFlee
// else show fail beat then startTiming() with sweetSpotFor(attemptsUsed) (D-12, D-26)
export function continueFromFlee(): void // same recap branch as continueFromResult (D-29)
```
Max attempts: 3 (D-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing retry/flee/recap flow tests</name>
  <files>pokemon-safari-app/src/hooks/useEncounterFlow.test.ts, pokemon-safari-app/src/game/capture.test.ts, pokemon-safari-app/src/screens/GameScreen.test.tsx</files>
  <read_first>
    - pokemon-safari-app/src/hooks/useEncounterFlow.test.ts
    - pokemon-safari-app/src/hooks/useEncounterFlow.ts
    - pokemon-safari-app/src/game/capture.test.ts
    - .planning/phases/05-capture-flow/05-CONTEXT.md (D-04, D-12, D-25, D-26, D-27, D-28, D-29, D-33)
    - .planning/phases/05-capture-flow/05-UI-SPEC.md (fail beat / flee copy — never destructive red framing)
    - .planning/phases/05-capture-flow/05-VALIDATION.md
  </read_first>
  <behavior>
    - Three consecutive failed rolls (stub rng always ≥ chance) reach stage flee; attemptsUsed === 3
    - After first fail, stage returns to timing with sweetSpot different from prior
    - continueFromFlee with wrong education → recap; with correct → close
    - continueFromResult with wrong education → recap (regression)
    - capture() during shake/flee is no-op
    - D-25 still holds; CATCH-05 distribution still holds
    - No Run button strings in overlay capture stages
  </behavior>
  <action>
Unskip/add useEncounterFlow describes for CATCH-04: force three fails with stubRng returning 0.999; assert flee stage and kind copy constants used by FleeCard tests or flow. Assert fail-then-timing remount changes sweetSpot (D-12). Assert D-29 for both continueFromResult and continueFromFlee. Keep capture.test.ts CATCH-05 / D-25 green assertions present (re-run, do not weaken thresholds). Add GameScreen or overlay assertion that flee body That’s okay — you’ll find another! can appear and Run does not.
  </action>
  <acceptance_criteria>
    - `grep -c "flee\|toFlee\|It got away" pokemon-safari-app/src/hooks/useEncounterFlow.test.ts` is ≥ 2
    - `grep -c "attemptsUsed\|Throw" pokemon-safari-app/src/hooks/useEncounterFlow.test.ts` is ≥ 1
    - `cd pokemon-safari-app && ! npx vitest run src/hooks/useEncounterFlow.test.ts -t flee` exits non-zero until Task 2 (or equivalent failing describe name)
  </acceptance_criteria>
  <verify>
    <automated>cd pokemon-safari-app && ! npx vitest run src/hooks/useEncounterFlow.test.ts -t "flee"</automated>
  </verify>
  <done>Retry/flee/recap contracts are expressed as failing or incomplete tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail beat, FleeCard, and recap Continue routing</name>
  <files>pokemon-safari-app/src/components/encounter/FleeCard.tsx, pokemon-safari-app/src/components/encounter/BallShake.tsx, pokemon-safari-app/src/components/encounter/EncounterOverlay.tsx, pokemon-safari-app/src/hooks/useEncounterFlow.ts, pokemon-safari-app/src/store/encounterStore.ts, pokemon-safari-app/src/data/educationConfig.ts, .planning/phases/05-capture-flow/05-VALIDATION.md</files>
  <read_first>
    - pokemon-safari-app/src/hooks/useEncounterFlow.test.ts
    - pokemon-safari-app/src/hooks/useEncounterFlow.ts
    - pokemon-safari-app/src/store/encounterStore.ts
    - pokemon-safari-app/src/components/encounter/CaughtCard.tsx
    - pokemon-safari-app/src/components/encounter/BallShake.tsx
    - pokemon-safari-app/src/components/encounter/RecapCard.tsx
    - pokemon-safari-app/src/data/rates.ts
    - .planning/phases/05-capture-flow/05-UI-SPEC.md (Flee card, fail beat, destructive forbidden)
    - .planning/phases/05-capture-flow/05-CONTEXT.md (D-04, D-12, D-26–D-29, D-33)
  </read_first>
  <behavior>
    - Same as Task 1; all flee/retry/recap tests green; full relevant suite green
  </behavior>
  <action>
Implement CATCH-04 end state (D-04, D-12, D-26–D-29, D-33):

1. onShakeComplete (or BallShake onComplete wired through flow): if lastCaught → toResult. Else increment attemptsUsed (ensure registerThrow did not double-count — single writer). If attemptsUsed &gt;= 3 → toFlee. Else show fail beat UI/copy Oh! It broke free! for encounterTimingMs.failBeat (reducedFailBeat under prefers-reduced-motion), then startTiming() which recomputes sweetSpot via sweetSpotFor(attemptsUsed) (D-12, D-26). No Try Again button. No Run / berry controls (D-27).

2. BallShake fail ending: ball opens / sprite pops briefly then fail beat (D-33). Success ending stays closed then CaughtCard (already from 05-02). Shake count remains flavor from lastChance bucket (D-30/D-31). Mirror AppearFlash presentational timer → onComplete (05-PATTERNS.md BallShake analog) — never call rollCapture here.

3. FleeCard: mirror RecapCard gba-dialog shell (05-PATTERNS.md FleeCard analog) — sprite optional, heading It got away!, body That’s okay — you’ll find another!, full-width primary PixelButton Continue → continueFromFlee. Never use destructive #C45C4A card chrome (UI-SPEC). Copy from captureCopy in educationConfig. No auto-advance (same as RecapCard).

4. continueFromFlee / continueFromResult: if session.education?.correct === false → toRecap(); else close() (D-29). Overlay mounts FleeCard on stage flee.

5. Re-run capture distribution tests; if CATCH-05 fails, retune only data/captureModifiers.timing and/or timingBar.zones / rarity bases — never component literals.

6. Update 05-VALIDATION.md: mark Wave 0 / per-task rows green where covered; set nyquist_compliant true only if all automated rows have commands and files exist.
  </action>
  <acceptance_criteria>
    - `cd pokemon-safari-app && npx vitest run src/hooks/useEncounterFlow.test.ts src/game/capture.test.ts src/components/encounter/TimingBar.test.tsx` exits 0
    - `grep -c "It got away!" pokemon-safari-app/src/components/encounter/FleeCard.tsx` equals 1
    - `grep -c "That’s okay\|That's okay" pokemon-safari-app/src/components/encounter/FleeCard.tsx` is ≥ 1
    - `grep -c "Oh! It broke free!" pokemon-safari-app/src/hooks/useEncounterFlow.ts pokemon-safari-app/src/data/educationConfig.ts` is ≥ 1
    - `grep -c "continueFromFlee" pokemon-safari-app/src/hooks/useEncounterFlow.ts` is ≥ 1
    - `grep -nE "\bRun\b" pokemon-safari-app/src/components/encounter/TimingBar.tsx pokemon-safari-app/src/components/encounter/FleeCard.tsx` finds no Run CTA
    - `grep -c "toRecap" pokemon-safari-app/src/hooks/useEncounterFlow.ts` is ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>cd pokemon-safari-app && npx vitest run src/hooks/useEncounterFlow.test.ts src/game/capture.test.ts src/components/encounter/TimingBar.test.tsx</automated>
  </verify>
  <done>Three fails flee kindly; retries remount timing; recap preserved; CATCH-05 gates hold. Full `npm test` deferred to wave/phase gate.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | Path |
|--------|------|------|
| `FleeCard` | component | `src/components/encounter/FleeCard.tsx` |
| `onShakeComplete` | function | `src/hooks/useEncounterFlow.ts` |
| `continueFromFlee` | function | `src/hooks/useEncounterFlow.ts` |
| `toFlee` | store action | `src/store/encounterStore.ts` |
| `captureCopy.flee` / `failBeat` | config strings | `src/data/educationConfig.ts` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attempt counter | Client-only; clamp attemptsUsed to [0,3] to avoid NaN flee skips |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | double-count attempts | mitigate | Single writer for attemptsUsed; capture() gated to timing |
| T-05-07 | Repudiation | N/A | accept | No audit log needed for local kid game |
| T-05-01 | Tampering | CATCH-05 retune | mitigate | Retune only via data/; config-surface still bans component literals |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
Focused flee/retry/capture vitest green; flee after 3 fails; fail beat remounts timing; Continue→recap when education wrong. Run `cd pokemon-safari-app && npm test` at wave/phase gate (not in Task 2 verify).
<human-check>
Fail three throws intentionally; confirm kind flee copy, no punishing red card, Continue returns to map or recap as appropriate.
</human-check>
</verification>

<success_criteria>
CATCH-04 retry/flee and D-29 recap path work; CATCH-05 distribution tests pass; Phase 5 capture loop is complete.
</success_criteria>

<output>
Create `.planning/phases/05-capture-flow/05-04-SUMMARY.md` when done
</output>
