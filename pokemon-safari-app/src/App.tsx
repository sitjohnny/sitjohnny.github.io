/**
 * Temporary Wave 0 stub — HashRouter shell + BottomNav land in plan 01-02.
 * Intentionally omits brand text and nav labels so Wave 0 tests fail until then.
 */
function App() {
  return (
    <main>
      <p>App scaffold ready</p>
    </main>
  )
}

export default App
