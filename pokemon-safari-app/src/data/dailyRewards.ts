/** Daily reward amounts — edit here only; UI must not hardcode these values. */

/** Phase 7 DAILY-01 consumes these amounts. */
export const dailyRewardAmounts = {
  pokeball: 5,
  greatball: 1,
  berry: 2,
} as const
